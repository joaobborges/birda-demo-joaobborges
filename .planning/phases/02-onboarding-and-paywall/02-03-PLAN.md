---
phase: 02-onboarding-and-paywall
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/(onboarding)/paywall.tsx
  - app/(onboarding)/reminders.tsx
  - app/(onboarding)/mailing-list.tsx
  - src/components/dev/DevPanel.tsx
  - app/(main)/index.tsx
  - app/(main)/profile.tsx
  - src/theme/components.ts
autonomous: true
requirements: [PAYW-01, PAYW-02, PAYW-03, STYL-01]

must_haves:
  truths:
    - "Paywall shows full-bleed hero image placeholder at top"
    - "Paywall headline includes user's name when available, falls back to generic"
    - "Plan toggle indicator width adapts to content via onLayout measurements"
    - "Nature Day variant shows different hero color and promotional banner with 20% off pricing"
    - "Reminders screen shows Remind me and Maybe later buttons"
    - "Mailing list screen shows toggle switch (off by default) with Save and Maybe later"
    - "All borderRadius properties in the app have accompanying borderCurve: continuous"
  artifacts:
    - path: "app/(onboarding)/paywall.tsx"
      provides: "Redesigned paywall with hero image, adaptive toggle, personalized headline"
    - path: "app/(onboarding)/reminders.tsx"
      provides: "Post-paywall reminders screen"
    - path: "app/(onboarding)/mailing-list.tsx"
      provides: "Post-paywall mailing list screen with toggle switch"
  key_links:
    - from: "app/(onboarding)/paywall.tsx"
      to: "src/stores/onboarding.ts"
      via: "useOnboardingStore to read name for headline"
      pattern: "useOnboardingStore.*name"
    - from: "app/(onboarding)/paywall.tsx"
      to: "app/(onboarding)/reminders.tsx"
      via: "router.push after subscribe/free"
      pattern: "push.*reminders"
    - from: "app/(onboarding)/mailing-list.tsx"
      to: "app/(main)"
      via: "completeOnboarding + router.replace"
      pattern: "completeOnboarding|replace.*main"
---

<objective>
Redesign the paywall with hero image, adaptive-width toggle, and personalization. Add 2 post-paywall screens (reminders, mailing list). Apply borderCurve: 'continuous' sweep across the entire app.

Purpose: Paywall is the conversion screen -- it must be polished with adaptive toggle and personalized headline. Post-paywall screens complete the flow before home. borderCurve ensures consistent iOS polish.
Output: Redesigned paywall, 2 new post-paywall screens, borderCurve applied globally.
</objective>

<execution_context>
@/Users/joaobborges/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaobborges/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-onboarding-and-paywall/02-CONTEXT.md
@.planning/phases/02-onboarding-and-paywall/02-RESEARCH.md
@.planning/phases/02-onboarding-and-paywall/02-01-SUMMARY.md

@app/(onboarding)/paywall.tsx
@src/components/onboarding/OnboardingLayout.tsx
@src/stores/onboarding.ts
@src/components/ui/Button.tsx
@src/components/dev/DevPanel.tsx
@src/theme/colors.ts

<interfaces>
<!-- From Plan 01 -->

From src/components/onboarding/OnboardingLayout.tsx:
```typescript
interface OnboardingLayoutProps {
  header?: ReactNode
  children: ReactNode
  footer?: ReactNode
}
export function OnboardingLayout({ header, children, footer }: OnboardingLayoutProps)
```

From src/stores/onboarding.ts (updated in Plan 01):
```typescript
interface OnboardingState {
  name: string
  birdingJourney: 'new' | 'garden' | 'intermediate' | 'expert' | null
  goals: string[]
  termsAccepted: boolean
  completed: boolean
  setName: (name: string) => void
  setBirdingJourney: (journey: OnboardingState['birdingJourney']) => void
  setGoals: (goals: string[]) => void
  setTermsAccepted: (accepted: boolean) => void
  completeOnboarding: () => void
  reset: () => void
}
```

From src/components/dev/DevPanel.tsx (existing):
- Already has Nature Day trigger via `variant: 'nature-day'` search param
- borderRadius: 22 at L94, MISSING borderCurve

STYL-01 audit from RESEARCH.md:
- DevPanel.tsx: borderRadius: 22 missing borderCurve
- app/(main)/index.tsx: multiple borderRadius instances, audit needed
- app/(main)/profile.tsx: multiple borderRadius instances, audit needed
- src/theme/components.ts: Button presets missing borderCurve
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign paywall with hero image, adaptive toggle, and personalization</name>
  <files>
    app/(onboarding)/paywall.tsx
  </files>
  <action>
Rewrite `app/(onboarding)/paywall.tsx` to use OnboardingLayout and implement the new design:

**Layout:**
- Use `OnboardingLayout` with NO header (no dots on paywall), NO footer (buttons in content area for full-bleed layout control)
- Actually, the paywall needs a full-bleed hero image at the top that extends edge-to-edge. OnboardingLayout has `paddingHorizontal: 24`. Two options:
  - Option A: Use OnboardingLayout but apply `marginHorizontal: -24` on the hero image to break out of padding
  - Option B: Don't use OnboardingLayout for paywall (it's a special screen)
  - Choose Option A to maintain ONBR-02 compliance. Apply negative margin on hero container only.

**Hero image (top):**
- Standard variant: `View` with `height: 220`, `backgroundColor: semantic.bgTinted` (blue50), `marginHorizontal: -24` to go edge-to-edge, `marginTop: -20` to reach top
- Nature Day variant: `backgroundColor: semantic.accent` (#2DD1E6), same dimensions
- Both have `borderBottomLeftRadius: 24`, `borderBottomRightRadius: 24`, `borderCurve: 'continuous'`

**Nature Day promotional banner:**
- Shown when `isNatureDay` is true
- Positioned overlapping bottom of hero image: `marginTop: -20`, centered
- Style: `backgroundColor: semantic.statusSuccessBg`, `paddingVertical: 8`, `paddingHorizontal: 16`, `borderRadius: 12`, `borderCurve: 'continuous'`, `alignSelf: 'center'`
- Text: "Nature Day -- 20% off today only" with leaf emoji

**Personalized headline:**
- `name ? \`${name}, unlock the full experience\` : 'Unlock the full experience'`
- Per user decision: name only, no skill level fallback
- Same styling as current title (fontSize: 30, fontWeight: '700')

**Adaptive-width toggle (PAYW-01):**
- Remove hardcoded `width: 148` from indicator
- Use `useSharedValue(0)` for both `monthlyWidth` and `annualWidth`
- Each toggle option wrapper gets `onLayout={(e) => { monthlyWidth.set(e.nativeEvent.layout.width) }}` (and annualWidth respectively)
- Remove `flex: 1` from `toggleOption` -- let options size to content with `paddingHorizontal: 20`
- Toggle container: `flexDirection: 'row'`, `alignSelf: 'center'` (not full-width), `backgroundColor: semantic.bgSurface`, `borderRadius: 16`, `borderCurve: 'continuous'`, `padding: 4`
- Indicator `useAnimatedStyle`:
  ```
  width: withSpring(plan === 'monthly' ? monthlyWidth.get() : annualWidth.get(), { damping: 15 })
  transform: [{ translateX: withSpring(plan === 'monthly' ? 0 : monthlyWidth.get(), { damping: 15 }) }]
  ```
- Remove old `togglePosition` shared value -- the plan state + measured widths drive everything
- Handle flash-on-mount: set indicator `opacity: 0` initially, set to `1` once both widths > 0 (check in useAnimatedStyle or use a `measured` shared value flag)

**Navigation change:**
- `handleSubscribe` and `handleFree` should NOT call `completeOnboarding()` yet -- there are 2 more screens
- Both navigate to `/(onboarding)/reminders` via `push` (not replace)
- Keep `isNatureDay` variant param forwarding if needed for post-paywall screens (not required per CONTEXT)

**Keep existing:**
- FEATURES array content
- Feature checkmark list rendering
- "Best value" badge on annual plan
- Button in footer area: "Start Free Trial" / "Claim My Discount" (primary) + "Continue with free plan" (link)

**Do NOT:**
- Import `useSafeAreaInsets` -- OnboardingLayout handles it
- Use `FadeInDown` -- use `FadeIn.delay(100).duration(300)` per animation decision
- Keep old `togglePosition` shared value approach
  </action>
  <verify>
    <automated>cd /Users/joaobborges/Developer/BIRDADEMO && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Paywall renders hero image placeholder (blue for standard, teal for Nature Day). Toggle indicator width adapts to content measurements. Headline shows user name when available. Nature Day shows promotional banner. Navigation goes to reminders (not main).</done>
</task>

<task type="auto">
  <name>Task 2: Create post-paywall screens and borderCurve sweep</name>
  <files>
    app/(onboarding)/reminders.tsx,
    app/(onboarding)/mailing-list.tsx,
    src/components/dev/DevPanel.tsx,
    app/(main)/index.tsx,
    app/(main)/profile.tsx,
    src/theme/components.ts
  </files>
  <action>
**1. Create `app/(onboarding)/reminders.tsx`:**
- Uses `OnboardingLayout` with NO header (no dots on post-paywall screens)
- Content: `Animated.View entering={FadeIn.delay(100).duration(300)}` wrapping:
  - Heading at top: "Stay in the loop" (fontSize: 24, fontWeight: '700', color: semantic.textPrimary, textAlign: 'center')
  - Description: "Get reminders about nearby bird activity and seasonal migrations" (fontSize: 16, color: semantic.textSecondary, textAlign: 'center', marginTop: 8)
  - Centered image placeholder: `View` with `height: 200`, `width: 200`, `backgroundColor: semantic.bgTinted`, `borderRadius: 100`, `borderCurve: 'continuous'`, `alignSelf: 'center'`, `marginVertical: 40`
- Footer: two buttons:
  - "Remind me" (primary) -- navigates to `/(onboarding)/mailing-list` (no actual reminder logic)
  - "Maybe later" (link) -- also navigates to `/(onboarding)/mailing-list`
- No state persistence needed (one-shot screen per RESEARCH.md recommendation)

**2. Create `app/(onboarding)/mailing-list.tsx`:**
- Uses `OnboardingLayout` with NO header
- Content: `Animated.View entering={FadeIn.delay(100).duration(300)}` wrapping:
  - Heading: "Join our mailing list" (fontSize: 24, fontWeight: '700', textAlign: 'center')
  - Description: "Get weekly birding tips, species spotlights, and community highlights" (fontSize: 16, color: semantic.textSecondary, textAlign: 'center', marginTop: 8)
  - Centered image placeholder: same as reminders (200x200 circle)
  - Toggle row below image: `View` with `flexDirection: 'row'`, `alignItems: 'center'`, `justifyContent: 'space-between'`, `paddingVertical: 16`
    - Label: "Subscribe to newsletter" (fontSize: 16, color: semantic.textPrimary)
    - `Switch` from react-native: `value={mailingList}`, `onValueChange={setMailingList}`, `trackColor={{ false: semantic.borderDefault, true: semantic.actionPrimary }}`, `thumbColor={semantic.bgPage}`
    - Use `useState(false)` for `mailingList` -- disabled by default
- Footer: two buttons:
  - "Save" (primary) -- calls `completeOnboarding()` then `replace('/(main)')` (this is where onboarding completes)
  - "Maybe later" (link) -- also calls `completeOnboarding()` then `replace('/(main)')`
- Import `useOnboardingStore` for `completeOnboarding`

**3. borderCurve sweep (STYL-01):**

Open each file below and add `borderCurve: 'continuous'` next to every `borderRadius` that does not already have it:

- `src/components/dev/DevPanel.tsx`: Add `borderCurve: 'continuous'` at L94 next to `borderRadius: 22`
- `app/(main)/index.tsx`: Audit ALL `StyleSheet.create` styles -- add `borderCurve: 'continuous'` next to every `borderRadius` that lacks it. Check styles for: map markers, info cards, buttons, badges, floating UI elements. Common borderRadius values in this file: check every one.
- `app/(main)/profile.tsx`: Same audit -- check avatar container, badge containers, achievement cards, any rounded elements.
- `src/theme/components.ts`: Add `borderCurve: 'continuous'` to button preset styles that have `borderRadius`.

**Rules for borderCurve sweep:**
- `borderCurve: 'continuous'` goes IMMEDIATELY after `borderRadius` in the style object (for readability)
- Android ignores this property -- safe to add everywhere, no platform check needed
- Do NOT add borderCurve where there is no borderRadius
- Also check the new files created in this plan (reminders.tsx, mailing-list.tsx, paywall.tsx) -- ensure they all have borderCurve where borderRadius is used (they should from the action instructions above)
  </action>
  <verify>
    <automated>cd /Users/joaobborges/Developer/BIRDADEMO && npx tsc --noEmit 2>&1 | head -5 && echo "---borderCurve audit---" && grep -rn "borderRadius" app/ src/ --include="*.tsx" --include="*.ts" | grep -v "borderCurve" | grep -v node_modules | grep -v ".planning" || echo "All borderRadius have borderCurve"</automated>
  </verify>
  <done>Reminders and mailing-list screens complete the flow to home. Mailing list calls completeOnboarding before navigating to main. borderCurve: continuous is present alongside every borderRadius in the entire app. The grep audit shows zero borderRadius without adjacent borderCurve.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Full navigation chain works: paywall -> reminders -> mailing-list -> home (main)
- Paywall toggle has no hardcoded width -- uses onLayout measurements
- Paywall headline includes name when store has a name
- Nature Day variant shows teal hero + promotional banner + 20% pricing
- `grep -rn "borderRadius" app/ src/ --include="*.tsx" --include="*.ts" | grep -v borderCurve` returns empty (STYL-01)
- Mailing list Switch defaults to off
- completeOnboarding is called only from mailing-list screen (last screen in flow)
</verification>

<success_criteria>
- Paywall has hero image, adaptive toggle, personalized headline (PAYW-01, PAYW-02)
- Nature Day variant accessible via DevPanel with 20% pricing (PAYW-03)
- Post-paywall screens (reminders, mailing list) complete the flow
- Every borderRadius in the app has borderCurve: continuous (STYL-01)
- Full 11-screen flow navigates from welcome to home without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-onboarding-and-paywall/02-03-SUMMARY.md`
</output>
