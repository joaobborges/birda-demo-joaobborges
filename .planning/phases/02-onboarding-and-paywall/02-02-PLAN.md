---
phase: 02-onboarding-and-paywall
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/(onboarding)/welcome.tsx
  - app/(onboarding)/ai-bird-id.tsx
  - app/(onboarding)/green-time.tsx
  - app/(onboarding)/discover.tsx
  - app/(onboarding)/community.tsx
  - app/(onboarding)/name.tsx
  - app/(onboarding)/birding-journey.tsx
  - app/(onboarding)/goals.tsx
  - app/(onboarding)/index.tsx
autonomous: true
requirements: [ONBR-01, ONBR-03]

must_haves:
  truths:
    - "User sees Welcome screen with hero image, T&C checkbox, and Create Account / Log in buttons"
    - "User navigates through 4 feature tour screens with progress dots (1/4 through 4/4)"
    - "User enters name via uncontrolled TextInput that does not re-render on keystrokes"
    - "User selects birding journey level from 4 chip options (single-select)"
    - "User selects goals from 5 multi-select options"
    - "Each screen content fades in with FadeIn animation after screen transition"
    - "Create Account and Log in buttons are disabled (opacity 0.5) when T&C checkbox is unchecked"
  artifacts:
    - path: "app/(onboarding)/welcome.tsx"
      provides: "Welcome screen with hero image, T&C, two buttons"
    - path: "app/(onboarding)/ai-bird-id.tsx"
      provides: "Feature tour screen 1 of 4"
    - path: "app/(onboarding)/green-time.tsx"
      provides: "Feature tour screen 2 of 4"
    - path: "app/(onboarding)/discover.tsx"
      provides: "Feature tour screen 3 of 4"
    - path: "app/(onboarding)/community.tsx"
      provides: "Feature tour screen 4 of 4"
    - path: "app/(onboarding)/name.tsx"
      provides: "Name input screen with uncontrolled TextInput"
    - path: "app/(onboarding)/birding-journey.tsx"
      provides: "Birding journey selection (4 levels, single-select chips)"
    - path: "app/(onboarding)/goals.tsx"
      provides: "Goals multi-select screen (5 options)"
  key_links:
    - from: "app/(onboarding)/welcome.tsx"
      to: "app/(onboarding)/ai-bird-id.tsx"
      via: "router.push on Create Account or Log in"
      pattern: "push.*ai-bird-id"
    - from: "app/(onboarding)/community.tsx"
      to: "app/(onboarding)/name.tsx"
      via: "router.push on Continue"
      pattern: "push.*name"
    - from: "app/(onboarding)/goals.tsx"
      to: "app/(onboarding)/paywall"
      via: "router.push on Continue"
      pattern: "push.*paywall"
    - from: "app/(onboarding)/name.tsx"
      to: "src/stores/onboarding.ts"
      via: "useRef + setName on Continue press"
      pattern: "nameRef\\.current|setName"
---

<objective>
Build all 8 onboarding screens (welcome through goals) using the OnboardingLayout from Plan 01. This covers the complete user journey from app launch to paywall.

Purpose: The onboarding flow is the first thing users see. These screens collect user preferences and showcase app features before the paywall.
Output: 8 screen files implementing the full pre-paywall onboarding flow.
</objective>

<execution_context>
@/Users/joaobborges/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaobborges/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-onboarding-and-paywall/02-CONTEXT.md
@.planning/phases/02-onboarding-and-paywall/02-RESEARCH.md
@.planning/phases/02-onboarding-and-paywall/02-01-SUMMARY.md

@src/components/onboarding/OnboardingLayout.tsx
@src/components/onboarding/ProgressDots.tsx
@src/components/ui/Button.tsx
@src/stores/onboarding.ts
@src/theme/colors.ts

<interfaces>
<!-- From Plan 01 (must exist before this plan runs) -->

From src/components/onboarding/OnboardingLayout.tsx:
```typescript
interface OnboardingLayoutProps {
  header?: ReactNode
  children: ReactNode
  footer?: ReactNode
}
export function OnboardingLayout({ header, children, footer }: OnboardingLayoutProps)
```

From src/stores/onboarding.ts (updated in Plan 01):
```typescript
interface OnboardingState {
  name: string
  birdingJourney: 'new' | 'garden' | 'intermediate' | 'expert' | null
  goals: string[]
  termsAccepted: boolean
  completed: boolean
  setName: (name: string) => void
  setBirdingJourney: (journey: OnboardingState['birdingJourney']) => void
  setGoals: (goals: string[]) => void
  setTermsAccepted: (accepted: boolean) => void
  completeOnboarding: () => void
  reset: () => void
}
```

From src/components/onboarding/ProgressDots.tsx:
```typescript
export function ProgressDots({ total, current }: { total: number; current: number })
```

From src/components/ui/Button.tsx:
```typescript
export function Button({ title, onPress, variant }: {
  title: string; onPress: () => void; variant?: 'primary' | 'secondary' | 'link'
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Welcome screen and 4 feature tour screens</name>
  <files>
    app/(onboarding)/welcome.tsx,
    app/(onboarding)/ai-bird-id.tsx,
    app/(onboarding)/green-time.tsx,
    app/(onboarding)/discover.tsx,
    app/(onboarding)/community.tsx,
    app/(onboarding)/index.tsx
  </files>
  <action>
**1. Rewrite `app/(onboarding)/welcome.tsx`:**
- Uses `OnboardingLayout` with NO header (no progress dots on welcome)
- Top half: full-bleed hero image placeholder -- use a `View` with `flex: 1`, `backgroundColor: semantic.bgTinted` (blue50), `borderRadius: 24`, `borderCurve: 'continuous'`
- Below image: "Welcome to Birda" heading (fontSize: 30, fontWeight: '700', color: semantic.textPrimary)
- Supporting text: "Discover, identify, and log birds around you" (fontSize: 16, color: semantic.textSecondary)
- T&C checkbox row: `Pressable` wrapping a checkbox `View` (20x20, borderRadius: 6, borderCurve: 'continuous', border 1.5px semantic.borderDefault; when checked: backgroundColor: semantic.actionPrimary with white checkmark "checkmark" text). Label: "I agree to the Terms of Service and Privacy Policy"
- Use local `useState` for `termsAccepted` (NOT the store -- store gets set when they actually proceed). Actually per CONTEXT.md the store has termsAccepted. Use `useOnboardingStore` for `termsAccepted` and `setTermsAccepted`.
- Footer: two buttons in the footer slot:
  - "Create Account" (variant: 'primary') -- disabled style when !termsAccepted (opacity: 0.5, onPress does nothing)
  - "Log in" (variant: 'link') -- also disabled when !termsAccepted
  - Both navigate to `/(onboarding)/ai-bird-id` when pressed and termsAccepted is true
- Content entrance animation: wrap main content (below hero) in `Animated.View entering={FadeIn.delay(100).duration(300)}`
- DO NOT import `useSafeAreaInsets` -- OnboardingLayout handles it

**2. Update `app/(onboarding)/index.tsx`:**
- Keep existing redirect logic -- should already point to `welcome`. Verify it redirects to `/(onboarding)/welcome`.

**3. Create 4 feature tour screens** -- all follow the SAME pattern:

Each screen uses `OnboardingLayout` with:
- `header`: `<ProgressDots total={4} current={N} />` where N is 0-3
- `footer`: `<Button title="Continue" onPress={() => push('/(onboarding)/NEXT')} />`
- `children`: `Animated.View entering={FadeIn.delay(100).duration(300)}` wrapping:
  - Image placeholder: `View` with `height: 280`, `backgroundColor: semantic.bgTinted`, `borderRadius: 24`, `borderCurve: 'continuous'`, `marginBottom: 32`
  - Heading: fontSize 28, fontWeight '700', color semantic.textPrimary, textAlign 'center'
  - Description: fontSize 16, color semantic.textSecondary, textAlign 'center', marginTop 12

Screen details:

| File | current | Heading | Description | Next route |
|------|---------|---------|-------------|------------|
| `ai-bird-id.tsx` | 0 | "AI Bird ID" | "Identify birds with AI photo recognition" | `green-time` |
| `green-time.tsx` | 1 | "Green Time" | "Turn your screen time into green time" | `discover` |
| `discover.tsx` | 2 | "Discover" | "Discover nearby birds and locations" | `community` |
| `community.tsx` | 3 | "Community" | "Join a community of bird watchers" | `name` |

- DO NOT import `useSafeAreaInsets` in any screen file
- Use `useRouter` destructured to `{ push }`
- `StyleSheet.create()` at bottom of each file
- Screen functions named with `Screen` suffix: `AIBirdIdScreen`, `GreenTimeScreen`, `DiscoverScreen`, `CommunityScreen`
- Export as `default function`
  </action>
  <verify>
    <automated>cd /Users/joaobborges/Developer/BIRDADEMO && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Welcome screen renders hero placeholder, T&C checkbox, and two buttons (disabled until T&C checked). All 4 tour screens render with ProgressDots, image placeholder, copy, and Continue button. Navigation chain: welcome -> ai-bird-id -> green-time -> discover -> community -> name.</done>
</task>

<task type="auto">
  <name>Task 2: Create name, birding-journey, and goals quiz screens</name>
  <files>
    app/(onboarding)/name.tsx,
    app/(onboarding)/birding-journey.tsx,
    app/(onboarding)/goals.tsx
  </files>
  <action>
**1. Modify `app/(onboarding)/name.tsx`:**
- Rewrite to use `OnboardingLayout`
- `header`: `<ProgressDots total={3} current={0} />` (quiz group: 1/3)
- Content: `Animated.View entering={FadeIn.delay(100).duration(300)}` wrapping:
  - Small image placeholder: `View` with `height: 120`, `width: 120`, `backgroundColor: semantic.bgTinted`, `borderRadius: 60`, `borderCurve: 'continuous'`, `alignSelf: 'center'`, `marginBottom: 24`
  - Heading: "What should we call you?" (fontSize: 24, fontWeight: '700', color: semantic.textPrimary, textAlign: 'center')
  - `TextInput` using `useRef` pattern (ONBR-03 requirement):
    - `const nameRef = useRef('')`
    - `onChangeText={(text) => { nameRef.current = text }}` -- no setState, no re-renders
    - Style: `backgroundColor: semantic.bgSurface`, `borderRadius: 16`, `borderCurve: 'continuous'`, `paddingHorizontal: 20`, `paddingVertical: 16`, `fontSize: 16`, `color: semantic.textPrimary`, `marginTop: 20`
    - `placeholder: "Your name"`, `placeholderTextColor: semantic.textMuted`
    - `defaultValue` from store: `defaultValue={useOnboardingStore.getState().name}` (read once, no subscription)
- Footer: two buttons:
  - "Continue" (primary) -- calls `setName(nameRef.current)` then `push('/(onboarding)/birding-journey')`
  - "Skip" (link) -- just `push('/(onboarding)/birding-journey')` WITHOUT setting name
- DO NOT import `useSafeAreaInsets`
- DO NOT use `useState` for the name input value

**2. Create `app/(onboarding)/birding-journey.tsx`:**
- Uses `OnboardingLayout`
- `header`: `<ProgressDots total={3} current={1} />` (quiz group: 2/3)
- Content: `Animated.View entering={FadeIn.delay(100).duration(300)}` wrapping:
  - Small image placeholder (same as name screen: 120x120 circle)
  - Heading: "Your birding journey" (fontSize: 24, fontWeight: '700', textAlign: 'center')
  - Description: "What best describes your experience?" (fontSize: 16, color: semantic.textSecondary, textAlign: 'center', marginTop: 8)
  - 4 chip/pill options laid out in a column (vertical stack, gap: 12, marginTop: 24):

```typescript
const LEVELS = [
  { key: 'new', label: 'New', description: "I'm new to birding" },
  { key: 'garden', label: 'Garden', description: 'I know a few garden birds' },
  { key: 'intermediate', label: 'Intermediate', description: 'I go birding regularly' },
  { key: 'expert', label: 'Expert', description: "I'm an experienced birder" },
] as const
```

  - Each chip: `Pressable` with `flexDirection: 'row'`, `paddingVertical: 16`, `paddingHorizontal: 20`, `borderRadius: 16`, `borderCurve: 'continuous'`, `borderWidth: 1.5`
  - Unselected: `borderColor: semantic.borderDefault`, `backgroundColor: semantic.bgPage`
  - Selected: `borderColor: semantic.actionPrimary`, `backgroundColor: semantic.actionPrimaryBg`
  - Chip text: label in bold (fontWeight: '600') + description in regular, or label as title line and description as subtitle
  - Single-select: use `useState` for local selected key, call `setBirdingJourney` on Continue
- Footer: "Continue" button (disabled/opacity 0.5 when nothing selected)
- `push('/(onboarding)/goals')` on Continue

**3. Create `app/(onboarding)/goals.tsx`:**
- Uses `OnboardingLayout`
- `header`: `<ProgressDots total={3} current={2} />` (quiz group: 3/3)
- Content: `Animated.View entering={FadeIn.delay(100).duration(300)}` wrapping:
  - Small image placeholder (120x120 circle)
  - Heading: "What are your goals?" (fontSize: 24, fontWeight: '700', textAlign: 'center')
  - 5 multi-select options (same chip style as birding-journey but multi-select):

```typescript
const GOALS = [
  'Spend more time outside',
  'Discover birding locations',
  'Identify birds',
  'Track my sightings',
  'Connect with other birders',
] as const
```

  - Multi-select: use `useState<string[]>([])`, toggle on press (add if not present, remove if present)
  - Call `setGoals(selectedGoals)` on Continue
- Footer: "Continue" button, navigates to `/(onboarding)/paywall`
- Continue button enabled even with no selections (goals are optional)
  </action>
  <verify>
    <automated>cd /Users/joaobborges/Developer/BIRDADEMO && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Name screen uses uncontrolled TextInput (useRef, no re-renders on keystroke). Birding journey shows 4 single-select chips. Goals shows 5 multi-select options. Navigation chain: name -> birding-journey -> goals -> paywall. All screens use OnboardingLayout with ProgressDots total={3}.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- All 8 screen files exist and export default functions
- No screen file imports `useSafeAreaInsets` (only OnboardingLayout does)
- Name screen uses `useRef` for TextInput (not useState)
- Navigation chain: welcome -> ai-bird-id -> green-time -> discover -> community -> name -> birding-journey -> goals -> paywall
- All screens use `Animated.View entering={FadeIn.delay(100).duration(300)}` for content entrance
- Feature tour dots: total={4}, current 0-3. Quiz dots: total={3}, current 0-2.
- Grep for `useSafeAreaInsets` in `app/(onboarding)/` returns only `_layout.tsx` (or nothing -- layout uses OnboardingLayout which is in src/)
</verification>

<success_criteria>
- Complete pre-paywall flow: welcome through goals (8 screens)
- All screens use OnboardingLayout (ONBR-02)
- Content fades in on each screen (ONBR-01)
- Name input uses uncontrolled ref pattern (ONBR-03)
- T&C checkbox gates welcome screen buttons
- Navigation is button-driven (no swipe)
</success_criteria>

<output>
After completion, create `.planning/phases/02-onboarding-and-paywall/02-02-SUMMARY.md`
</output>
