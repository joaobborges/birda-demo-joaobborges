---
phase: 03-map-and-screens
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [app/(main)/index.tsx, app/(main)/community.tsx, app/(main)/_layout.tsx]
autonomous: true
gap_closure: true
requirements: [MAP-02, SCRN-02]

must_haves:
  truths:
    - "All floating navigation icons use filled (non-outline) Ionicons variants"
    - "Community feed displays sighting cards in vertical layout with full-width hero image"
    - "Community screen back button shows a plain arrow with no 'index' text"
  artifacts:
    - path: "app/(main)/index.tsx"
      provides: "Floating nav with filled icons"
      contains: "person-circle"
    - path: "app/(main)/community.tsx"
      provides: "Social feed with vertical card layout and hero images"
      contains: "height: 200"
    - path: "app/(main)/_layout.tsx"
      provides: "Clean header navigation for community"
      contains: "headerBackTitle"
  key_links:
    - from: "app/(main)/index.tsx"
      to: "Ionicons"
      via: "filled icon name props"
      pattern: "name=\"(person-circle|people|notifications|camera|book)\""
    - from: "app/(main)/_layout.tsx"
      to: "community screen"
      via: "headerBackTitle empty string"
      pattern: "headerBackTitle.*''"
---

<objective>
Close 2 UAT gaps from Phase 03: (1) switch all floating nav icons from outline to filled variants, (2) restructure community feed to a proper social-feed vertical card layout with hero images and fix the back button label.

Purpose: Address user-reported cosmetic and major issues from UAT testing.
Output: Patched index.tsx, community.tsx, and _layout.tsx files.
</objective>

<execution_context>
@/Users/joaobborges/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaobborges/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-map-and-screens/03-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Switch floating nav icons to filled variants and fix back button</name>
  <files>app/(main)/index.tsx, app/(main)/_layout.tsx</files>
  <action>
In app/(main)/index.tsx, change these Ionicons name props from outline to filled:
- Line 141: "person-circle-outline" -> "person-circle"
- Line 145: "people-outline" -> "people"
- Line 148: "notifications-outline" -> "notifications"
- Line 158: "camera-outline" -> "camera"
- Line 163: "book-outline" -> "book"

In app/(main)/_layout.tsx, add headerBackTitle: '' to the community screen options to prevent the back button from showing "index" text. Also add headerBackTitle: '' to the profile screen options for consistency:

```tsx
<Stack.Screen
  name="profile"
  options={{
    headerShown: true,
    title: 'Profile',
    headerBackTitle: '',
  }}
/>
<Stack.Screen
  name="community"
  options={{
    headerShown: true,
    title: 'Community Sightings',
    headerBackTitle: '',
  }}
/>
```
  </action>
  <verify>
    <automated>cd /Users/joaobborges/Developer/BIRDADEMO && grep -c "outline" app/\(main\)/index.tsx | grep -q "^[012]$" && echo "PASS: no more than 2 outline refs" || echo "FAIL: too many outline icons"; grep "headerBackTitle" app/\(main\)/_layout.tsx && echo "PASS: headerBackTitle set" || echo "FAIL: missing headerBackTitle"</automated>
  </verify>
  <done>All 5 floating nav icons use filled Ionicons variants (no -outline suffix). Back button on community and profile screens shows only the arrow icon with no text label.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure community feed to vertical social-feed card layout</name>
  <files>app/(main)/community.tsx</files>
  <action>
Redesign the SightingItem component in app/(main)/community.tsx from the current horizontal row layout (flexDirection: row with 90x100 thumbnail) to a vertical social-feed card layout:

1. Card structure (top to bottom):
   - Full-width hero image: height 200, width '100%', borderTopLeftRadius and borderTopRightRadius matching card radius (16). Use contentFit="cover".
   - Content section below image with padding 14:
     - Bird name (bold, 16px) and species (italic, 13px) on first line
     - Location row: Ionicons "location-sharp" icon (size 13, textMuted color) + location text, marginTop 6
     - Bottom row (flexDirection: row, justifyContent: space-between, marginTop 8):
       - Left: username + dot + timestamp
       - Right: heart-outline icon + like count

2. Card container styles:
   - Remove flexDirection: 'row' from item style
   - Keep backgroundColor, borderRadius: 16, borderCurve: 'continuous', overflow: 'hidden', boxShadow

3. Update birdImage style:
   - width: '100%' (remove fixed 90)
   - height: 200 (replace 100)
   - Remove borderRadius from image (card overflow hidden handles top corners)

4. Add a short description to each sighting in the SIGHTINGS data array. Add a `description` field to the Sighting interface and data:
   - id 1: "Spotted this little robin hopping around the park benches"
   - id 2: "Incredible flash of blue along the stream"
   - id 3: "Nesting pair right by the tower"
   - id 4: "First time seeing a hoopoe in the wild!"
   - id 5: "Huge flock at sunset, amazing sight"
   - id 6: "Caught this colorful visitor near the olive grove"

5. Render the description text below the bird name/species, styled as fontSize 14, color textBody, marginTop 6.

Maintain borderCurve: 'continuous' on the card (STYL-01 convention).
  </action>
  <verify>
    <automated>cd /Users/joaobborges/Developer/BIRDADEMO && grep -q "height: 200" app/\(main\)/community.tsx && echo "PASS: hero image height" || echo "FAIL: no hero image"; grep -q "location-sharp" app/\(main\)/community.tsx && echo "PASS: location icon" || echo "FAIL: no location icon"; grep -q "description" app/\(main\)/community.tsx && echo "PASS: description field" || echo "FAIL: no description"</automated>
  </verify>
  <done>Community feed displays cards in a vertical social-feed layout: full-width 200px hero image on top, bird name, short description, location with icon, and username/timestamp/likes on the bottom row. No horizontal row layout remains.</done>
</task>

</tasks>

<verification>
1. All 5 floating nav Ionicons in index.tsx use filled names (no "-outline" suffix)
2. Community and profile screens show plain back arrow with no "index" label
3. Community feed cards show vertical layout with large hero image, description, and location icon
4. borderCurve: 'continuous' maintained on all rounded elements
</verification>

<success_criteria>
- Zero "-outline" icon names on the 5 floating nav buttons in index.tsx
- Community SightingItem renders vertically: 200px hero image, description text, location with icon
- Back button on community screen shows no text label
- App builds and renders without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-map-and-screens/03-03-SUMMARY.md`
</output>
