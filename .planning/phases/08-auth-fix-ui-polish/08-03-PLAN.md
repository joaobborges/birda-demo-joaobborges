---
phase: 08-auth-fix-ui-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/profile.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Profile achievements display as a vertical list of full-width rows"
    - "4 earned achievements show in full color with their original icons"
    - "3 locked achievements appear dimmed with lock icons"
  artifacts:
    - path: "app/profile.tsx"
      provides: "Vertical achievement list with earned + locked items"
      contains: "unlocked"
  key_links:
    - from: "app/profile.tsx"
      to: "src/theme/colors.ts"
      via: "semantic tokens for backgrounds and text"
      pattern: "semantic\\."
---

<objective>
Restructure profile achievements from a 4-column grid to a vertical list with locked placeholder items.

Purpose: Show achievement progression more clearly with earned items in full color and locked items dimmed, per user decision.
Output: Vertical achievement list with 4 earned + 3 locked items.
</objective>

<execution_context>
@/Users/joaobborges/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaobborges/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-auth-fix-ui-polish/08-CONTEXT.md
@.planning/phases/08-auth-fix-ui-polish/08-RESEARCH.md

<interfaces>
<!-- Current profile achievements structure -->
From app/profile.tsx:
```typescript
const ACHIEVEMENTS = [
  { icon: 'sunny-outline' as const, label: 'Early Bird', bg: semantic.rarityUncommonBg },
  { icon: 'eye-outline' as const, label: 'Sharp Eye', bg: semantic.rarityCommonBg },
  { icon: 'camera-outline' as const, label: 'Photographer', bg: semantic.rarityRareBg },
  { icon: 'compass-outline' as const, label: 'Explorer', bg: semantic.statusSuccessBg },
]

// Current layout — flexDirection: 'row' (4 columns)
achievements: { flexDirection: 'row', gap: spacing['3'] }
achievement: { flex: 1, alignItems: 'center', gap: spacing['2'], ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure achievements to vertical list with locked items</name>
  <files>app/profile.tsx</files>
  <action>
Replace the achievements grid with a vertical list layout:

**1. Update ACHIEVEMENTS data array** — add `unlocked` boolean and 3 locked items:
```tsx
const ACHIEVEMENTS = [
  { icon: 'sunny-outline' as const, label: 'Early Bird', bg: semantic.rarityUncommonBg, unlocked: true },
  { icon: 'eye-outline' as const, label: 'Sharp Eye', bg: semantic.rarityCommonBg, unlocked: true },
  { icon: 'camera-outline' as const, label: 'Photographer', bg: semantic.rarityRareBg, unlocked: true },
  { icon: 'compass-outline' as const, label: 'Explorer', bg: semantic.statusSuccessBg, unlocked: true },
  { icon: 'lock-closed-outline' as const, label: 'Night Owl', bg: semantic.bgSurface, unlocked: false },
  { icon: 'lock-closed-outline' as const, label: 'Migration Tracker', bg: semantic.bgSurface, unlocked: false },
  { icon: 'lock-closed-outline' as const, label: 'Rare Finder', bg: semantic.bgSurface, unlocked: false },
]
```

**2. Replace the achievements rendering** — change from grid to vertical list:
```tsx
<View style={styles.achievements}>
  {ACHIEVEMENTS.map((a) => (
    <View key={a.label} style={[styles.achievementRow, !a.unlocked && styles.achievementRowLocked]}>
      <View style={[styles.achievementIcon, { backgroundColor: a.bg }]}>
        <Ionicons name={a.icon} size={24} color={a.unlocked ? semantic.textPrimary : semantic.textMuted} />
      </View>
      <Text style={styles.achievementLabel}>{a.label}</Text>
    </View>
  ))}
</View>
```

**3. Replace achievement styles** — remove old `achievement` style, add new row styles:
- `achievements`: Change from `flexDirection: 'row'` to `gap: spacing['3']` only (vertical column is default)
- Add `achievementRow`: `{ flexDirection: 'row', alignItems: 'center', gap: spacing['4'], paddingVertical: spacing['3'], paddingHorizontal: spacing['4'], borderRadius: 16, borderCurve: 'continuous', backgroundColor: semantic.bgPage, boxShadow: '0 2px 8px rgba(0, 0, 0, 0.08)' }`
- Add `achievementRowLocked`: `{ opacity: 0.4 }` (Claude's discretion: 0.4 opacity — clearly dimmed but readable)
- `achievementIcon`: Keep existing 44x44 circle style unchanged
- `achievementLabel`: Update to use `fontSize: 14` (slightly larger since it's a row, not a tiny column) and remove `textAlign` if any

Remove the old `achievement` style (the flex: 1 column card style).
  </action>
  <verify>
    <automated>cd /Users/joaobborges/Developer/BIRDADEMO && npx expo lint 2>&1 | head -20</automated>
  </verify>
  <done>Profile achievements render as full-width rows. 4 earned achievements in full color with original icons. 3 locked achievements at 0.4 opacity with lock-closed-outline icons. Vertical list layout replaces horizontal grid.</done>
</task>

</tasks>

<verification>
- `npx expo lint` passes with no errors
- Visual: Achievements display as vertical list rows
- Visual: Earned items show full color, locked items appear dimmed
- Visual: Lock icon on locked items, original icons on earned items
</verification>

<success_criteria>
- ACHIEVEMENTS array has 7 items (4 unlocked + 3 locked)
- Achievements container uses vertical layout (no flexDirection: 'row')
- Each achievement is a horizontal row (icon + label)
- Locked items have 0.4 opacity and lock-closed-outline icon
- Earned items retain original icons and background colors
</success_criteria>

<output>
After completion, create `.planning/phases/08-auth-fix-ui-polish/08-03-SUMMARY.md`
</output>
