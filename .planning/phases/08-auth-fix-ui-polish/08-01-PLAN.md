---
phase: 08-auth-fix-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(onboarding)/paywall.tsx
  - src/components/map/BirdMarker.tsx
  - app/(main)/_layout.tsx
  - app/(main)/index.tsx
  - app/_layout.tsx
autonomous: true
requirements: [PAYW-06, NAV-03, NAV-04]

must_haves:
  truths:
    - "Paywall social proof boxes display '400+ happy birders' and '4.7 stars' text content"
    - "Paywall unlock pill shows ribbon icon instead of lock-open-outline"
    - "Paywall Nature Day banner shows only text with no leaf icon or emoji"
    - "Map markers have 44x44px minimum tap target while visual size remains unchanged"
    - "Active tab bar color is primary blue (#1F87FE)"
    - "Map header icons (profile, notification) use neutral400 color matching inactive tab items"
    - "Profile screen header has no border/separator line"
    - "Profile screen back button shows only chevron with no text label"
    - "Community screen header has no border/separator line"
  artifacts:
    - path: "app/(onboarding)/paywall.tsx"
      provides: "Social proof content, ribbon icon, cleaned banner"
      contains: "400+ happy birders"
    - path: "src/components/map/BirdMarker.tsx"
      provides: "44x44 transparent hit area wrapper"
      contains: "hitArea"
    - path: "app/(main)/_layout.tsx"
      provides: "Tab active color + community header shadow removal"
      contains: "actionPrimary"
    - path: "app/(main)/index.tsx"
      provides: "Header icon color change to neutral400"
      contains: "neutral400"
    - path: "app/_layout.tsx"
      provides: "Profile header shadow removal + back button display mode"
      contains: "headerShadowVisible"
  key_links:
    - from: "app/(main)/_layout.tsx"
      to: "src/theme/colors.ts"
      via: "semantic.actionPrimary import"
      pattern: "semantic\\.actionPrimary"
---

<objective>
Apply paywall content fixes, map marker hit area enlargement, and navigation styling polish.

Purpose: Close PAYW-06 (social proof content), NAV-03/NAV-04 (marker tap target — drawer already works), and polish tab bar + header styling across the app.
Output: Paywall with real social proof text, larger map marker tap targets, blue active tab color, consistent header icon colors, clean headers without separator lines.
</objective>

<execution_context>
@/Users/joaobborges/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaobborges/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-auth-fix-ui-polish/08-CONTEXT.md
@.planning/phases/08-auth-fix-ui-polish/08-RESEARCH.md

<interfaces>
<!-- Key imports and patterns the executor needs -->

From src/theme/colors.ts:
```typescript
export const semantic = { actionPrimary: '#1F87FE', ... }
export const colors = { neutral400: '...', neutral700: '...', ... }
```

From src/theme/typography.ts:
```typescript
export const fontWeights = { regular: '...', semiBold: '...', bold: '...' }
```

From src/theme/spacing.ts:
```typescript
export const spacing = { '1': 4, '2': 8, '3': 12, '4': 16, '5': 20, '6': 24, ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Paywall content fixes — social proof, icon, banner</name>
  <files>app/(onboarding)/paywall.tsx</files>
  <action>
Three changes in paywall.tsx:

1. **Social proof boxes** (lines 73-78): Replace empty `<View style={styles.socialBox} />` elements with content. Each box gets centered text as placeholder until user provides images:
   - First box: `<View style={styles.socialBox}><Text style={styles.socialText}>400+ happy birders</Text></View>`
   - Second box: `<View style={styles.socialBox}><Text style={styles.socialText}>4.7 stars</Text></View>`
   - Add `socialText` style: `{ fontFamily: fontWeights.semiBold, fontSize: 13, color: semantic.textSecondary, textAlign: 'center', padding: spacing['3'] }`
   - Add `alignItems: 'center'` and `justifyContent: 'center'` to existing `socialBox` style so text centers vertically

2. **Unlock pill icon** (line 82): Change `name="lock-open-outline"` to `name="ribbon"` on the Ionicons component.

3. **Nature Day banner** (lines 49-54):
   - Remove the `<Ionicons name="leaf" size={16} color="#FFFFFF" />` element entirely
   - Remove the emoji from the text: change `🌿 Nature Day Special —` to just `Nature Day Special —`
   - Keep the banner container, gap style, and bold "10% off" text unchanged
  </action>
  <verify>
    <automated>cd /Users/joaobborges/Developer/BIRDADEMO && npx expo lint 2>&1 | head -20</automated>
  </verify>
  <done>Paywall shows "400+ happy birders" and "4.7 stars" in social proof boxes, unlock pill uses ribbon icon, Nature Day banner has no leaf icon or emoji</done>
</task>

<task type="auto">
  <name>Task 2: Map marker hit area + tab/navigation styling</name>
  <files>src/components/map/BirdMarker.tsx, app/(main)/_layout.tsx, app/(main)/index.tsx, app/_layout.tsx</files>
  <action>
Four files, each with focused changes:

**BirdMarker.tsx** — Add 44x44 transparent hit area wrapper:
- Wrap each marker variant (common, uncommon, rare) in a parent `<View style={styles.hitArea}>` that is 44x44px with `alignItems: 'center'` and `justifyContent: 'center'`
- The existing marker views render centered inside the hit area
- Add `hitArea` style: `{ width: 44, height: 44, alignItems: 'center', justifyContent: 'center' }`
- Keep `anchor={{ x: 0.5, y: 0.5 }}` on Marker unchanged — the 44x44 wrapper remains centered on the coordinate

**app/(main)/_layout.tsx** — Tab active color + community header:
- Update import: `import { colors, semantic } from '@/theme/colors'` (add `semantic`)
- Change `tabBarActiveTintColor: colors.neutral700` to `tabBarActiveTintColor: semantic.actionPrimary`
- On the Community `Tabs.Screen`, add `headerShadowVisible: false` to options

**app/(main)/index.tsx** — Map header icon colors:
- Line 161: Change `color={semantic.textPrimary}` to `color={colors.neutral400}` on the person-circle Ionicons
- Line 164: Change `color={semantic.textPrimary}` to `color={colors.neutral400}` on the notifications Ionicons
- `colors` is already imported on this file

**app/_layout.tsx** — Profile header polish:
- On the Stack.Screen for "profile" (line 63), add to options: `headerShadowVisible: false` and `headerBackButtonDisplayMode: 'minimal'`
  </action>
  <verify>
    <automated>cd /Users/joaobborges/Developer/BIRDADEMO && npx expo lint 2>&1 | head -20</automated>
  </verify>
  <done>Map markers have 44x44 tap targets, active tab is blue, header icons are neutral400, profile/community headers have no separator line, profile back button shows only chevron</done>
</task>

</tasks>

<verification>
- `npx expo lint` passes with no errors
- Visual: Paywall social proof boxes show text content
- Visual: Map markers are easier to tap
- Visual: Active tab is blue, header icons are lighter gray
- Visual: No header separator lines on Profile or Community
</verification>

<success_criteria>
- Paywall social proof boxes contain "400+ happy birders" and "4.7 stars" text
- Unlock pill shows ribbon icon
- Nature Day banner has no leaf icon or emoji
- BirdMarker wraps all variants in 44x44 hit area
- Tab bar active color is semantic.actionPrimary (#1F87FE)
- Map header icons use colors.neutral400
- Profile header: headerShadowVisible false, headerBackButtonDisplayMode minimal
- Community header: headerShadowVisible false
</success_criteria>

<output>
After completion, create `.planning/phases/08-auth-fix-ui-polish/08-01-SUMMARY.md`
</output>
