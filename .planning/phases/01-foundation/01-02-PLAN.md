---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/theme/colors.ts
  - src/theme/typography.ts
  - app/(onboarding)/welcome.tsx
  - app/(onboarding)/name.tsx
  - app/(onboarding)/location.tsx
  - app/(onboarding)/interests.tsx
  - app/(onboarding)/skill-level.tsx
  - app/(onboarding)/notifications.tsx
  - app/(onboarding)/summary.tsx
  - app/(onboarding)/paywall.tsx
  - app/(main)/index.tsx
  - app/(main)/community.tsx
  - app/(main)/profile.tsx
  - src/components/dev/DevPanel.tsx
  - src/components/onboarding/ProgressDots.tsx
  - src/components/ui/Button.tsx
autonomous: true
requirements:
  - FOUN-01

must_haves:
  truths:
    - "All color values in screen and component files reference centralized theme tokens -- zero hardcoded hex strings"
    - "All screen backgrounds use pure white (#FFFFFF via semantic.bgPage) -- no warm off-whites (#FAFAF8)"
    - "All green-based colors (#1B4332, #2D6A4F) replaced with birda blue palette tokens"
    - "Rubik font is applied via typography tokens wherever font family is used"
    - "Rarity badge backgrounds (common/uncommon/rare) use dedicated rarity tokens"
    - "Status colors (error red, success green bg) use dedicated status tokens"
  artifacts:
    - path: "src/theme/colors.ts"
      provides: "Extended color system with accent, secondary, rarity, status, and additional neutral tokens"
      contains: "rarityCommonBg"
    - path: "app/(onboarding)/welcome.tsx"
      provides: "Token-migrated welcome screen"
      exports: ["default"]
    - path: "app/(main)/index.tsx"
      provides: "Token-migrated map screen"
      exports: ["default"]
  key_links:
    - from: "app/(onboarding)/*.tsx"
      to: "src/theme/colors.ts"
      via: "import { semantic } or { colors } from '@/theme/colors'"
      pattern: "from '@/theme"
    - from: "app/(main)/*.tsx"
      to: "src/theme/colors.ts"
      via: "import { semantic } or { colors } from '@/theme/colors'"
      pattern: "from '@/theme"
    - from: "src/components/**/*.tsx"
      to: "src/theme/colors.ts"
      via: "import { semantic } or { colors } from '@/theme/colors'"
      pattern: "from '@/theme"
---

<objective>
Extend the theme token system with missing color tokens (accent, secondary, rarity, status, extended neutrals), then sweep all 14 screen and component files to replace every hardcoded hex string with token references. Apply Rubik font globally in the same pass.

Purpose: Single source of truth for all styling -- no hardcoded hex values remain in any screen or component file.
Output: Extended colors.ts with full token coverage, all 14 files migrated to use tokens exclusively.
</objective>

<execution_context>
@/Users/joaobborges/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaobborges/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Current theme token system that will be extended -->

From src/theme/colors.ts:
```typescript
export const colors = {
  blue50: '#C7E1FF',
  blue100: '#A3CEFF',
  blue500: '#1F87FE',
  neutral900: '#111111',
  neutral800: '#292929',
  neutral700: '#2D3142',
  neutral500: '#787878',
  neutral300: '#D9D9D9',
  neutral100: '#F8F8F8',
  neutral0: '#FFFFFF',
} as const;

export const semantic = {
  textPrimary: colors.neutral700,
  textSecondary: colors.neutral500,
  textStrong: colors.neutral900,
  textInverse: colors.neutral0,
  bgPage: colors.neutral0,
  bgSurface: colors.neutral100,
  bgTinted: colors.blue50,
  borderDefault: colors.neutral300,
  borderSubtle: colors.neutral100,
  actionPrimary: colors.blue500,
  actionPrimaryHover: colors.blue100,
  actionPrimaryBg: colors.blue50,
} as const;

export type ColorToken = keyof typeof colors;
export type SemanticToken = keyof typeof semantic;
```

From src/theme/typography.ts (font family):
```typescript
export const fontFamily = {
  regular: 'Rubik_400Regular',
  medium: 'Rubik_500Medium',
  semiBold: 'Rubik_600SemiBold',
  bold: 'Rubik_700Bold',
} as const;
```

From src/theme/index.ts (barrel -- exempt from no-barrel-files):
```typescript
export { colors, semantic } from './colors';
export { fontFamily, typography } from './typography';
export { spacing, widgetSpacing } from './spacing';
export { borderRadius, buttons } from './components';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend theme tokens with missing colors</name>
  <files>src/theme/colors.ts</files>
  <action>
Extend `src/theme/colors.ts` with the missing tokens identified in RESEARCH.md. Preserve ALL existing tokens and types unchanged -- only ADD new entries.

**New entries in `colors` object:**
- `accent: '#2DD1E6'` -- Teal accent (birda.org brand)
- `secondary: '#7265E3'` -- Purple secondary (birda.org brand)
- `neutral950: '#000000'` -- Pure black (button backgrounds)
- `neutral400: '#9CA3AF'` -- Placeholder text, muted icons
- `neutral650: '#374151'` -- Strong body text
- `statusError: '#EF4444'` -- Error, notification badges
- `statusSuccessBg: '#D4EDDA'` -- Success background
- `rarityCommonBg: '#DBEAFE'` -- Common rarity badge
- `rarityUncommonBg: '#FEF3C7'` -- Uncommon rarity badge
- `rarityRareBg: '#FCE7F3'` -- Rare rarity badge

**New entries in `semantic` object:**
- `textMuted: colors.neutral400` -- Placeholder text, muted labels
- `textBody: colors.neutral650` -- Strong body text
- `textInput: colors.neutral800` -- Input field text (map to existing neutral800 for #1F2937)
- `accent: colors.accent` -- Teal accent usage
- `secondary: colors.secondary` -- Purple secondary usage
- `actionSecondary: colors.neutral950` -- Black button backgrounds
- `actionSecondaryText: colors.neutral0` -- White text on black buttons
- `statusError: colors.statusError` -- Error color
- `statusSuccessBg: colors.statusSuccessBg` -- Success background
- `rarityCommonBg: colors.rarityCommonBg`
- `rarityUncommonBg: colors.rarityUncommonBg`
- `rarityRareBg: colors.rarityRareBg`

Keep `as const` assertions and type exports. Organize new entries with clear section comments matching existing style (e.g., `// -- Accent --`, `// -- Status --`, `// -- Rarity --`).
  </action>
  <verify>
    <automated>cd /Users/joaobborges/Developer/BIRDADEMO && node -e "const c = require('./src/theme/colors'); const checks = ['accent','secondary','neutral950','neutral400','statusError','rarityCommonBg'].every(k => c.colors[k]); const sem = ['textMuted','actionSecondary','statusError','rarityCommonBg','accent','secondary'].every(k => c.semantic[k]); console.log(checks && sem ? 'PASS' : 'FAIL')"</automated>
  </verify>
  <done>colors.ts has all new color tokens (accent, secondary, extended neutrals, status, rarity) and corresponding semantic mappings. Existing tokens unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate all 14 files from hardcoded hex to theme tokens</name>
  <files>
    app/(onboarding)/welcome.tsx,
    app/(onboarding)/name.tsx,
    app/(onboarding)/location.tsx,
    app/(onboarding)/interests.tsx,
    app/(onboarding)/skill-level.tsx,
    app/(onboarding)/notifications.tsx,
    app/(onboarding)/summary.tsx,
    app/(onboarding)/paywall.tsx,
    app/(main)/index.tsx,
    app/(main)/community.tsx,
    app/(main)/profile.tsx,
    src/components/dev/DevPanel.tsx,
    src/components/onboarding/ProgressDots.tsx,
    src/components/ui/Button.tsx
  </files>
  <action>
For each of the 14 files, replace EVERY hardcoded hex string with the appropriate theme token import. Read each file, identify all hex values, map them to tokens, and rewrite.

**Import pattern:** Add `import { semantic, colors } from '@/theme/colors'` (or just `{ semantic }` if only semantic tokens needed). Use `@/theme/colors` direct import, NOT the barrel `@/theme/index.ts`.

**Color mapping rules (from RESEARCH.md and user decisions):**
- `#FAFAF8` (warm off-white page bg) -> `semantic.bgPage` (#FFFFFF -- user decision: pure white for ALL page backgrounds)
- `#F8F8F8` / `#F3F4F6` (surface bg) -> `semantic.bgSurface` (only for cards/secondary surfaces)
- `#1B4332` (dark green heading) -> `semantic.textPrimary` (birda navy #2D3142)
- `#2D6A4F` (medium green active) -> `semantic.actionPrimary` (birda blue #1F87FE)
- `#2D3142` -> `semantic.textPrimary`
- `#1F87FE` -> `semantic.actionPrimary`
- `#2DD1E6` -> `semantic.accent`
- `#7265E3` -> `semantic.secondary`
- `#FFFFFF` -> `semantic.bgPage` or `semantic.textInverse` (context-dependent)
- `#000000` -> `semantic.actionSecondary` (button bg) or `colors.neutral950` (text)
- `#9CA3AF` -> `semantic.textMuted`
- `#6B7280` -> `semantic.textSecondary` (close enough to neutral500)
- `#374151` -> `semantic.textBody`
- `#1F2937` -> `semantic.textInput`
- `#24292F` -> `semantic.textStrong`
- `#E5E7EB` -> `semantic.borderDefault`
- `#D9D9D9` -> `semantic.borderDefault`
- `#EF4444` -> `semantic.statusError`
- `#D4EDDA` -> `semantic.statusSuccessBg`
- `#DBEAFE` -> `semantic.rarityCommonBg`
- `#FEF3C7` -> `semantic.rarityUncommonBg`
- `#FCE7F3` -> `semantic.rarityRareBg`
- `#C7E1FF` -> `semantic.bgTinted`
- `#A3CEFF` -> `semantic.actionPrimaryHover`

**Switch trackColor (notifications.tsx):**
- `true` track: `semantic.actionPrimary` (blue, not green)
- `false` track: `semantic.borderDefault`

**Font migration:** Where any file uses fontFamily strings directly (e.g., `fontFamily: 'System'` or hardcoded font names), replace with `fontFamily` import from `@/theme/typography`. Add import: `import { fontFamily } from '@/theme/typography'`.

**Rules:**
- Do NOT introduce new hardcoded hex values. If a color has no token, add it to colors.ts first (but Task 1 should cover all needed tokens).
- Preserve all component logic, layout, and behavior -- only change color/font references.
- Keep `StyleSheet.create()` pattern at bottom of files.
- After migration, run: `grep -rn '#[0-9a-fA-F]\{3,8\}' app/ src/components/ --include='*.tsx'` -- must return ZERO results.
  </action>
  <verify>
    <automated>cd /Users/joaobborges/Developer/BIRDADEMO && count=$(grep -rn '#[0-9a-fA-F]\{3,8\}' app/ src/components/ --include='*.tsx' 2>/dev/null | wc -l | tr -d ' '); echo "Remaining hex values: $count"; test "$count" -eq 0 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Zero hardcoded hex strings remain in any screen or component .tsx file. All 14 files import from @/theme/colors (and @/theme/typography where needed). Green colors replaced with birda blue palette. Page backgrounds are pure white.</done>
</task>

</tasks>

<verification>
1. `cd /Users/joaobborges/Developer/BIRDADEMO && grep -rn '#[0-9a-fA-F]\{3,8\}' app/ src/components/ --include='*.tsx' | wc -l` -- must be 0
2. `cd /Users/joaobborges/Developer/BIRDADEMO && grep -rn "from '@/theme" app/ src/components/ --include='*.tsx' | wc -l` -- should be >= 14 (every migrated file imports from theme)
3. `cd /Users/joaobborges/Developer/BIRDADEMO && node -e "const c = require('./src/theme/colors'); console.log(Object.keys(c.semantic).length >= 20 ? 'Token count OK' : 'FAIL')"` -- confirms token extension
</verification>

<success_criteria>
- src/theme/colors.ts extended with accent, secondary, rarity (3), status (2), and extended neutral (3+) tokens plus semantic mappings
- All 14 files (8 onboarding + 3 main + 3 components) import colors from @/theme/colors
- grep for hardcoded hex in app/ and src/components/ returns zero results
- All page backgrounds use semantic.bgPage (pure white #FFFFFF) -- no #FAFAF8
- All green colors (#1B4332, #2D6A4F) replaced with appropriate birda blue tokens
- Rubik font applied via typography tokens where font family is referenced
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
