---
phase: 14-ui-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/welcome/AuthDrawer.tsx
  - src/components/onboarding/ProgressDots.tsx
  - app/(main)/_layout.tsx
  - app/(main)/capture.tsx
autonomous: true
requirements:
  - UFIX-01
  - UFIX-02
  - UFIX-03

must_haves:
  truths:
    - "Auth drawer backdrop covers the entire welcome screen including the mosaic grid"
    - "Inactive progress dots on onboarding screens are blue at 50% opacity"
    - "Tab bar shows exactly 3 tabs: Map, Community, Logbook — no Capture tab"
  artifacts:
    - path: "src/components/welcome/AuthDrawer.tsx"
      provides: "Full-screen backdrop fix"
      contains: "StyleSheet.absoluteFillObject"
    - path: "src/components/onboarding/ProgressDots.tsx"
      provides: "Blue inactive dots"
      contains: "rgba"
  key_links:
    - from: "app/(main)/_layout.tsx"
      to: "app/(main)/capture.tsx"
      via: "Capture tab removal"
      pattern: "capture.tsx deleted"
---

<objective>
Fix three visual defects: auth drawer backdrop coverage, onboarding stepper dot colors, and remove the Capture tab from the tab bar.

Purpose: Correct visual bugs identified in v1.1 review before adding new FAB feature.
Output: Three small fixes across four files, one file deleted.
</objective>

<execution_context>
@/Users/joaobborges/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaobborges/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-ui-fixes/14-CONTEXT.md

@src/components/welcome/AuthDrawer.tsx
@src/components/onboarding/ProgressDots.tsx
@app/(main)/_layout.tsx
@app/(onboarding)/welcome.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix auth backdrop and stepper dot colors</name>
  <files>src/components/welcome/AuthDrawer.tsx, src/components/onboarding/ProgressDots.tsx</files>
  <action>
In `src/components/welcome/AuthDrawer.tsx`:
The BottomSheetBackdrop already has `style={[props.style, StyleSheet.absoluteFillObject]}` — but the backdrop is rendered inside the BottomSheet which is a child of the welcome screen's `View`. The backdrop should cover the full screen. The current implementation looks correct with `StyleSheet.absoluteFillObject`, so verify whether the issue is that the BottomSheet component constrains the backdrop. If the backdrop still does not cover the mosaic, the fix is to ensure the AuthDrawer's BottomSheet parent container fills the entire screen. Check whether the BottomSheet's `style` prop or a `containerStyle` prop is needed to make the backdrop fill the screen. Likely fix: add `style={StyleSheet.absoluteFillObject}` directly to the BottomSheet component, or use the `containerStyle` prop if available. The key observable: when the auth drawer opens on the welcome screen, the dark overlay must visually cover the mosaic grid at the top.

In `src/components/onboarding/ProgressDots.tsx`:
Change the inactive dot background color from `'rgba(255, 255, 255, 0.5)'` to `'rgba(31, 135, 254, 0.5)'` (which is `#1F87FE` at 50% opacity). This is the semantic.actionPrimary color in rgba form.
  </action>
  <verify>
    <automated>cd /Users/joaobborges/Developer/BIRDADEMO && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>ProgressDots inactive color is rgba(31, 135, 254, 0.5). AuthDrawer backdrop renders as full-screen overlay covering the mosaic grid when opened on the welcome screen.</done>
</task>

<task type="auto">
  <name>Task 2: Remove Capture tab from tab bar</name>
  <files>app/(main)/_layout.tsx, app/(main)/capture.tsx</files>
  <action>
In `app/(main)/_layout.tsx`:
Remove the entire `<Tabs.Screen name="capture" ... />` block (lines 34-42). The resulting tab bar has 3 tabs: Map (index), Community, Logbook — in that order.

Delete the file `app/(main)/capture.tsx` entirely to avoid dead code.

Do NOT add any FAB-related code in this task — that is handled in Plan 02.
  </action>
  <verify>
    <automated>cd /Users/joaobborges/Developer/BIRDADEMO && ! test -f app/\(main\)/capture.tsx && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>capture.tsx is deleted. Tab bar layout renders exactly 3 tabs: Map, Community, Logbook. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- ProgressDots.tsx inactive style contains `rgba(31, 135, 254, 0.5)`
- `_layout.tsx` has no reference to "capture"
- `capture.tsx` file does not exist
</verification>

<success_criteria>
1. Auth backdrop covers full welcome screen including mosaic when drawer opens
2. Inactive onboarding dots are blue (#1F87FE) at 50% opacity
3. Tab bar shows Map | Community | Logbook with no Capture tab
4. No dead code (capture.tsx deleted)
5. TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/14-ui-fixes/14-01-SUMMARY.md`
</output>
