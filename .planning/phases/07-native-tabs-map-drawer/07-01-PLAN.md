---
phase: 07-native-tabs-map-drawer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(main)/_layout.tsx
  - app/(main)/index.tsx
  - app/(main)/capture.tsx
  - app/(main)/logbook.tsx
  - app/(main)/community.tsx
autonomous: true
requirements:
  - NAV-01

must_haves:
  truths:
    - "Home screen shows a native iOS tab bar with Map, Community, Capture, and Logbook tabs"
    - "Map is the default/active tab on launch"
    - "Tapping Community tab shows the existing social feed screen"
    - "Capture and Logbook tabs show placeholder screens"
    - "Floating bottom bar is removed from the map screen"
    - "Community icon is removed from the floating top bar; Profile and Notification remain"
  artifacts:
    - path: "app/(main)/_layout.tsx"
      provides: "NativeTabs layout with 4 triggers"
      contains: "NativeTabs"
    - path: "app/(main)/capture.tsx"
      provides: "Capture placeholder tab screen"
      contains: "Coming soon"
    - path: "app/(main)/logbook.tsx"
      provides: "Logbook placeholder tab screen"
      contains: "Coming soon"
  key_links:
    - from: "app/(main)/_layout.tsx"
      to: "app/(main)/index.tsx"
      via: "NativeTabs.Trigger name='index'"
      pattern: "NativeTabs\\.Trigger.*name=\"index\""
    - from: "app/(main)/_layout.tsx"
      to: "app/(main)/community.tsx"
      via: "NativeTabs.Trigger name='community'"
      pattern: "NativeTabs\\.Trigger.*name=\"community\""
---

<objective>
Convert the main navigation from a custom floating bottom bar to a native iOS tab bar using Expo Router's `unstable-native-tabs`, create placeholder Capture and Logbook tab screens, and clean up the map screen's floating overlays.

Purpose: NAV-01 requires a native iOS tab bar. This replaces the custom floating bottom bar with a true UITabBarController and restructures the main layout.
Output: Working native tab bar with 4 tabs, placeholder screens for Capture/Logbook, cleaned-up map screen.
</objective>

<execution_context>
@/Users/joaobborges/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaobborges/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-native-tabs-map-drawer/07-CONTEXT.md
@.planning/phases/07-native-tabs-map-drawer/07-RESEARCH.md

@app/(main)/_layout.tsx
@app/(main)/index.tsx
@app/(main)/community.tsx
@src/theme/colors.ts
@src/theme/typography.ts
@src/theme/spacing.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/theme/colors.ts:
```typescript
export const colors = {
  neutral400: '#9CA3AF', // inactive tab icons
  neutral700: '#2D3142', // active tab icons (dark navy)
  // ...
}
export const semantic = {
  textPrimary: colors.neutral700,
  textSecondary: colors.neutral500,
  bgPage: colors.neutral0,
  // ...
}
```

From src/theme/typography.ts:
```typescript
export const fontWeights = {
  regular: 'Rubik_400Regular',
  medium: 'Rubik_500Medium',
  semiBold: 'Rubik_600SemiBold',
  bold: 'Rubik_700Bold',
}
export const typography = {
  h2: { fontFamily: 'Rubik_600SemiBold', fontSize: 22, lineHeight: 28 },
  bodySmall: { fontFamily: 'Rubik_400Regular', fontSize: 14, lineHeight: 20 },
  // ...
}
```

From src/theme/spacing.ts:
```typescript
export const spacing = { '1': 4, '2': 8, '3': 12, '4': 16, '5': 20, '6': 24, /* ... */ }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert main layout to NativeTabs and create placeholder tab screens</name>
  <files>app/(main)/_layout.tsx, app/(main)/capture.tsx, app/(main)/logbook.tsx</files>
  <action>
1. **Rewrite `app/(main)/_layout.tsx`** — Replace the current `Stack` navigator with `NativeTabs` from `expo-router/unstable-native-tabs`. Configure 4 tab triggers in this exact order: Map (index), Community, Capture, Logbook.

```tsx
import { NativeTabs } from 'expo-router/unstable-native-tabs'
import { colors } from '@/theme/colors'
```

Tab configuration:
- `NativeTabs.Trigger name="index"` with `sf="map.fill"` and label "Map"
- `NativeTabs.Trigger name="community"` with `sf="person.2.fill"` and label "Community"
- `NativeTabs.Trigger name="capture"` with `sf="camera.fill"` and label "Capture"
- `NativeTabs.Trigger name="logbook"` with `sf="book.fill"` and label "Logbook"

Set `iconColor={{ default: colors.neutral400, selected: colors.neutral700 }}` on `NativeTabs` for inactive gray / active dark navy per user decision.

Do NOT include `profile.tsx` as a tab trigger — it stays as a push screen. If NativeTabs needs explicit screen exclusion, check if `href={null}` pattern works or if profile routing needs to move outside the `(main)` group. The research notes NativeTabs children are triggers only — profile push may need to be routed from root Stack instead. Test `router.push('/(main)/profile')` still works after conversion; if not, move `profile.tsx` outside `(main)` to `app/profile.tsx`.

2. **Create `app/(main)/capture.tsx`** — Minimal placeholder screen: centered camera icon (Ionicons `camera-outline`, size 48, color `semantic.textMuted`) + "Capture" title (typography.h2) + "Coming soon" subtitle (typography.bodySmall, color textSecondary). Use `semantic.bgPage` background. Use `SafeAreaView` or `useSafeAreaInsets` for padding. Use `StyleSheet.create()` and `borderCurve: 'continuous'` on any rounded corners.

3. **Create `app/(main)/logbook.tsx`** — Same pattern as capture.tsx but with Ionicons `book-outline` icon and "Logbook" title.

Both placeholder screens should be simple, lightweight, and use design system tokens.
  </action>
  <verify>
    <automated>cd /Users/joaobborges/Developer/BIRDADEMO && npx expo export --platform ios --dump-sourcemap 2>&1 | head -20 || npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>NativeTabs layout renders with 4 tabs in correct order. Capture and Logbook show placeholder content. Profile push still navigable.</done>
</task>

<task type="auto">
  <name>Task 2: Clean up map screen — remove floating bottom bar and community icon</name>
  <files>app/(main)/index.tsx</files>
  <action>
In `app/(main)/index.tsx`:

1. **Remove the entire floating bottom bar** — Delete the `Animated.View` block with `styles.bottomBar` (the Capture/Logbook floating buttons around lines 157-170). Delete the corresponding styles: `bottomBar`, `bottomButton`, `bottomButtonContent`, `bottomButtonText`.

2. **Remove community icon from floating top bar** — In the `topBarRight` View, remove the Pressable that pushes to `/community` (the `people` icon Pressable around lines 146-148). Keep the Profile icon (left side) and Notification icon (right side) as floating overlays on the map.

3. **Remove the `BirdInfoCard` conditional render** (lines 172-175) and the `BirdInfoCard` import — this will be replaced by the bottom sheet drawer in Plan 02. Keep the `selectedBird` state and `setSelectedBird` — Plan 02 will use them. Also keep the `BirdMarker` `onPress={setSelectedBird}` wiring.

4. **Remove unused imports** after cleanup: `BirdInfoCard` import. Keep `Ionicons` (used by notification icon). Keep all map-related imports.

Do NOT remove the `topBar` styles or the profile/notification icons — those stay as floating overlays per user decision.
  </action>
  <verify>
    <automated>cd /Users/joaobborges/Developer/BIRDADEMO && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Map screen has no floating bottom bar, no community icon in top bar, no BirdInfoCard. Profile and notification icons remain. selectedBird state preserved for Plan 02.</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors (`npx tsc --noEmit`)
- 4 tabs visible in the native tab bar (Map, Community, Capture, Logbook)
- Map tab is active by default
- Community tab shows the existing social feed
- Capture and Logbook tabs show placeholder content
- No floating bottom bar on map screen
- No community icon in floating top bar
- Profile and notification floating icons still visible on map
</verification>

<success_criteria>
Native iOS tab bar renders with 4 tabs using SF Symbol icons. Map is the default tab. The custom floating bottom bar and community top-bar icon are removed. Placeholder screens exist for Capture and Logbook.
</success_criteria>

<output>
After completion, create `.planning/phases/07-native-tabs-map-drawer/07-01-SUMMARY.md`
</output>
