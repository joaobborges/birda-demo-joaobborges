---
phase: 02-onboarding-and-paywall
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/onboarding/OnboardingLayout.tsx
  - src/components/ui/Button.tsx
  - src/theme/components.ts
  - app/(onboarding)/_layout.tsx
  - app/(onboarding)/welcome.tsx
  - app/(onboarding)/ai-bird-id.tsx
  - app/(onboarding)/green-time.tsx
  - app/(onboarding)/discover.tsx
  - app/(onboarding)/community.tsx
  - app/(onboarding)/goals.tsx
  - app/(onboarding)/paywall.tsx
autonomous: true
gap_closure: true
requirements: [ONBR-01, ONBR-02, PAYW-01, STYL-01]

must_haves:
  truths:
    - "Illustration/image area extends full-bleed to screen edges and behind status bar on all onboarding screens"
    - "ProgressDots and status bar overlay on top of the illustration area"
    - "Footer buttons are pinned to the bottom of the screen, always visible above scrollable content"
    - "Status bar area is transparent so hero images show through"
    - "Login button on welcome screen has same dimensions as Create Account (ghost variant)"
  artifacts:
    - path: "src/components/onboarding/OnboardingLayout.tsx"
      provides: "Two-zone layout: full-bleed illustration slot + padded scrollable content + pinned footer"
      contains: "ScrollView"
    - path: "src/components/ui/Button.tsx"
      provides: "Ghost button variant with same container dimensions as primary"
      contains: "ghost"
    - path: "src/theme/components.ts"
      provides: "Ghost button token in buttons map"
      contains: "ghost"
    - path: "app/(onboarding)/_layout.tsx"
      provides: "Translucent status bar configuration"
      contains: "statusBarTranslucent"
  key_links:
    - from: "app/(onboarding)/ai-bird-id.tsx"
      to: "OnboardingLayout"
      via: "illustration prop"
      pattern: "illustration="
    - from: "OnboardingLayout"
      to: "ScrollView"
      via: "children wrapped in ScrollView, footer outside"
      pattern: "ScrollView"
    - from: "welcome.tsx"
      to: "Button"
      via: "ghost variant for login"
      pattern: "variant.*ghost"
---

<objective>
Restructure OnboardingLayout for full-bleed illustrations and pinned footer, add ghost button variant.

Purpose: Close 4 major UAT gaps -- full-bleed images behind status bar, pinned bottom buttons, transparent status bar, and properly sized login button.
Output: Rearchitected OnboardingLayout with illustration slot, all onboarding screens updated to use new layout zones, ghost Button variant.
</objective>

<execution_context>
@/Users/joaobborges/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaobborges/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-onboarding-and-paywall/02-UAT.md

<interfaces>
<!-- Current OnboardingLayout interface (will be extended) -->
From src/components/onboarding/OnboardingLayout.tsx:
```typescript
interface OnboardingLayoutProps {
  header?: ReactNode
  children: ReactNode
  footer?: ReactNode
}
```

From src/components/ui/Button.tsx:
```typescript
interface ButtonProps {
  title: string
  onPress: () => void
  variant?: 'primary' | 'secondary' | 'link'
}
```

From src/theme/spacing.ts:
```typescript
export const spacing = {
  '1': 4, '2': 8, '3': 12, '4': 16, '5': 20, '6': 24, '8': 32, ...
}
```

From src/theme/components.ts:
```typescript
export const buttons = {
  default: { paddingVertical: 16, paddingHorizontal: 32, ... },
  cta: { ... },
  small: { ... },
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure OnboardingLayout, add ghost Button, configure translucent status bar</name>
  <files>src/components/onboarding/OnboardingLayout.tsx, src/components/ui/Button.tsx, src/theme/components.ts, app/(onboarding)/_layout.tsx</files>
  <action>
**OnboardingLayout.tsx** - Restructure into two-zone architecture:
1. Add `illustration` prop (ReactNode) to OnboardingLayoutProps for the full-bleed top zone.
2. Remove `paddingHorizontal: 24` and `paddingTop: top + 20` from the outer container. The outer container should be `flex: 1, backgroundColor: semantic.bgPage` only.
3. Layout structure (top to bottom in a single flex column):
   - Illustration zone: `{illustration ? <View style={{ position: 'relative' }}>{illustration}<View style={{ position: 'absolute', top: top + 12, left: 0, right: 0, zIndex: 1 }}>{header}</View></View> : header ? <View style={{ paddingTop: top + 20, paddingHorizontal: 24 }}>{header}</View> : null}`
   - The illustration zone has NO padding, NO borderRadius -- it bleeds to all edges including behind the status bar.
   - When no illustration but header exists (e.g. name screen), apply the old paddingTop + paddingHorizontal to header wrapper only.
   - Content zone: Wrap `children` in a `ScrollView` with `contentContainerStyle: { flexGrow: 1, paddingHorizontal: 24 }` and `showsVerticalScrollIndicator: false`. The ScrollView gets `flex: 1` in its style.
   - Footer zone: Stays OUTSIDE the ScrollView. Apply `paddingHorizontal: 24, paddingBottom: bottom + 20, gap: 8` to footer View. This keeps buttons pinned at the bottom always.
4. Import ScrollView from react-native.

**Button.tsx** - Add ghost variant:
1. Extend variant type to `'primary' | 'secondary' | 'ghost' | 'link'`.
2. Ghost variant renders the same container as primary/secondary (uses `styles.button` base) but with `backgroundColor: 'transparent'` and no border. Text color: `semantic.textSecondary`.
3. Add `styles.ghost: { backgroundColor: 'transparent' }` and `styles.ghostText: { color: semantic.textSecondary }`.
4. In the render logic, add ghost alongside primary/secondary in the container branch (NOT in the link branch that renders bare text).

**components.ts** - Add ghost token:
1. Add `ghost: { paddingVertical: 16, paddingHorizontal: 32, backgroundColor: 'transparent', borderRadius: borderRadius.full, borderCurve: 'continuous' as const }` to the buttons map.
2. Update ButtonVariant type export.

**_layout.tsx** - Translucent status bar:
1. Import `StatusBar` from `expo-status-bar`.
2. Add `<StatusBar style="dark" translucent />` as a sibling to the Stack component (wrap both in a Fragment).
3. Add `statusBarTranslucent: true` to Stack screenOptions. Note: this ensures Android also renders content behind the status bar.
  </action>
  <verify>
    <automated>cd /Users/joaobborges/Developer/BIRDADEMO && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>OnboardingLayout accepts illustration prop with full-bleed zone, children wrapped in ScrollView, footer pinned outside scroll. Button has ghost variant. Status bar configured translucent.</done>
</task>

<task type="auto">
  <name>Task 2: Update all onboarding screens to use full-bleed illustration slot and pinned footer</name>
  <files>app/(onboarding)/welcome.tsx, app/(onboarding)/ai-bird-id.tsx, app/(onboarding)/green-time.tsx, app/(onboarding)/discover.tsx, app/(onboarding)/community.tsx, app/(onboarding)/goals.tsx, app/(onboarding)/paywall.tsx</files>
  <action>
**Feature tour screens (ai-bird-id, green-time, discover, community):**
1. Move the imagePlaceholder View out of children and into the `illustration` prop on OnboardingLayout.
2. The illustration View: `height: '50%'` (fills roughly top half of screen), `backgroundColor: semantic.bgTinted`, NO borderRadius, NO borderCurve. Full width is automatic since the illustration zone has no horizontal padding.
3. Remove imagePlaceholder from local styles. Remove `marginBottom: 32` from it.
4. Children now contain only the heading and description text (already wrapped in Animated.View with FadeIn). Add `paddingTop: 24` to give spacing below the illustration zone.

**Welcome screen:**
1. Move heroPlaceholder into `illustration` prop. Style: `height: '45%'` (slightly less than tour screens to leave room for more content), `backgroundColor: semantic.bgTinted`, NO borderRadius.
2. Remove old `heroPlaceholder` style with `flex:1, borderRadius:24`.
3. Fix footer: change Login button from `variant="link"` to `variant="ghost"`.
4. Add `gap: spacing['1']` (4px) to the footer wrapper View (between Create Account and Login). Import spacing from `@/theme/spacing`.
5. Add `marginBottom: spacing['4']` (16px) to the checkboxRow style so there is clear separation between checkbox and the footer buttons below.
6. Children content: add `paddingTop: 24` at the top of the Animated.View.

**Goals screen:**
1. Replace the circular avatarPlaceholder with a full-bleed illustration via the `illustration` prop. Style: `height: '40%'`, `backgroundColor: semantic.bgTinted`, NO borderRadius.
2. Remove the avatarPlaceholder View and its style from children.
3. Children: add `paddingTop: 24` spacing. Keep heading, options as-is.

**Paywall screen:**
1. Move the hero View into the `illustration` prop. Style: `height: 220`, `backgroundColor: isNatureDay ? semantic.accent : semantic.bgTinted`, NO borderRadius, NO marginHorizontal, NO marginTop. Remove the old negative margin hack.
2. Move the action buttons (Subscribe + free plan) from inline content into the `footer` prop on OnboardingLayout. This pins them to the bottom.
3. Remove `styles.hero` and `styles.actions` from local StyleSheet.
4. Children: the rest of the content (banner, title, features, toggle) stays in children. Add `paddingTop: 24`.
5. The scrollContent wrapper still uses `gap: 24` for internal spacing.
  </action>
  <verify>
    <automated>cd /Users/joaobborges/Developer/BIRDADEMO && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All 7 screens use OnboardingLayout illustration prop for full-bleed images. Footer buttons pinned on all screens. No borderRadius on illustration zones. Images extend behind status bar.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors.
2. Visual check: each onboarding screen shows illustration bleeding to screen edges and behind status bar.
3. Visual check: progress dots and status bar overlay on top of illustration.
4. Visual check: footer buttons stay pinned at bottom, do not scroll away.
5. Visual check: welcome screen Login button has same dimensions as Create Account (ghost style).
</verification>

<success_criteria>
- OnboardingLayout has illustration prop rendering a full-bleed zone with no padding or border radius
- Header/ProgressDots overlay on illustration via absolute positioning
- Children wrapped in ScrollView with flex:1
- Footer pinned outside ScrollView at bottom
- Ghost button variant exists with same container as primary
- Status bar is translucent on both platforms
- All 7 affected screens updated to use illustration prop
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-onboarding-and-paywall/02-04-SUMMARY.md`
</output>
