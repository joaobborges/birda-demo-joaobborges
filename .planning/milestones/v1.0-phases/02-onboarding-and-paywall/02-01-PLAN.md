---
phase: 02-onboarding-and-paywall
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/onboarding/OnboardingLayout.tsx
  - src/stores/onboarding.ts
  - app/(onboarding)/_layout.tsx
  - app/(onboarding)/location.tsx
  - app/(onboarding)/interests.tsx
  - app/(onboarding)/notifications.tsx
  - app/(onboarding)/summary.tsx
  - app/(onboarding)/skill-level.tsx
autonomous: true
requirements: [ONBR-02]

must_haves:
  truths:
    - "OnboardingLayout renders header/children/footer slots with safe area insets handled internally"
    - "Stack navigator uses fade animation instead of slide_from_right"
    - "Zustand store has birdingJourney, goals, and termsAccepted fields"
    - "Old unused screen files are removed from the project"
  artifacts:
    - path: "src/components/onboarding/OnboardingLayout.tsx"
      provides: "Shared layout component with header/children/footer slots"
      exports: ["OnboardingLayout"]
    - path: "src/stores/onboarding.ts"
      provides: "Updated onboarding state with new fields"
      exports: ["useOnboardingStore"]
    - path: "app/(onboarding)/_layout.tsx"
      provides: "Stack navigator with fade animation"
  key_links:
    - from: "src/components/onboarding/OnboardingLayout.tsx"
      to: "react-native-safe-area-context"
      via: "useSafeAreaInsets internal to layout"
      pattern: "useSafeAreaInsets"
    - from: "src/stores/onboarding.ts"
      to: "zustand"
      via: "create with persist middleware"
      pattern: "birdingJourney|goals|termsAccepted"
---

<objective>
Create the foundation components for the new 11-screen onboarding flow: shared layout component, updated Zustand store, stack animation config, and old screen cleanup.

Purpose: Every onboarding screen in Plans 02 and 03 depends on OnboardingLayout and the updated store. This must land first.
Output: OnboardingLayout component, updated store, fade animation config, cleaned up file tree.
</objective>

<execution_context>
@/Users/joaobborges/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaobborges/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-onboarding-and-paywall/02-CONTEXT.md
@.planning/phases/02-onboarding-and-paywall/02-RESEARCH.md

@src/stores/onboarding.ts
@src/components/onboarding/ProgressDots.tsx
@src/components/ui/Button.tsx
@src/theme/colors.ts
@app/(onboarding)/_layout.tsx

<interfaces>
<!-- Existing interfaces the executor needs -->

From src/stores/onboarding.ts:
```typescript
interface OnboardingState {
  name: string
  location: string | null
  skillLevel: 'beginner' | 'intermediate' | 'advanced' | null
  interests: string[]
  completed: boolean
  setName: (name: string) => void
  setLocation: (location: string) => void
  setSkillLevel: (level: OnboardingState['skillLevel']) => void
  setInterests: (interests: string[]) => void
  completeOnboarding: () => void
  reset: () => void
}
```

From src/theme/colors.ts:
```typescript
export const semantic = {
  bgPage: colors.neutral0,       // #FFFFFF
  bgSurface: colors.neutral100,  // #F8F8F8
  textPrimary: colors.neutral700, // #2D3142
  // ... (full list in file)
}
```

From src/components/onboarding/ProgressDots.tsx:
```typescript
interface ProgressDotsProps {
  total: number
  current: number
}
export function ProgressDots({ total, current }: ProgressDotsProps)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OnboardingLayout, update store, configure fade animation</name>
  <files>
    src/components/onboarding/OnboardingLayout.tsx,
    src/stores/onboarding.ts,
    app/(onboarding)/_layout.tsx
  </files>
  <action>
**1. Create `src/components/onboarding/OnboardingLayout.tsx`:**
- Named export `OnboardingLayout` (not default -- follows component convention)
- Props interface `OnboardingLayoutProps` with: `header?: ReactNode`, `children: ReactNode`, `footer?: ReactNode`
- Uses `useSafeAreaInsets()` internally -- screens must NEVER import or call useSafeAreaInsets
- Container: `flex: 1`, `backgroundColor: semantic.bgPage`, `paddingHorizontal: 24`, `paddingTop: top + 20`, `paddingBottom: bottom + 20`
- Content area (children wrapper): `flex: 1`, `justifyContent: 'center'`
- Footer area: `gap: 8` (for stacked buttons)
- Use ternary (not `&&`) for conditional header/footer rendering: `{header ? header : null}`
- `StyleSheet.create()` at bottom of file

**2. Update `src/stores/onboarding.ts`:**
- Replace `skillLevel` field with `birdingJourney: 'new' | 'garden' | 'intermediate' | 'expert' | null`
- Replace `interests: string[]` with `goals: string[]`
- Add `termsAccepted: boolean` (default: `false`)
- Replace `setSkillLevel` with `setBirdingJourney: (journey: OnboardingState['birdingJourney']) => void`
- Replace `setInterests` with `setGoals: (goals: string[]) => void`
- Add `setTermsAccepted: (accepted: boolean) => void`
- Remove `location` field and `setLocation` action (location screen removed from flow)
- Update `reset()` to clear new fields: `birdingJourney: null, goals: [], termsAccepted: false`
- Keep `name`, `completed`, `setName`, `completeOnboarding`, `reset` unchanged
- Keep persist middleware config unchanged (storage key: 'onboarding-storage')

**3. Update `app/(onboarding)/_layout.tsx`:**
- Change `animation: 'slide_from_right'` to `animation: 'fade'`
- Keep `gestureEnabled: false` and `headerShown: false`
  </action>
  <verify>
    <automated>cd /Users/joaobborges/Developer/BIRDADEMO && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>OnboardingLayout component exists with header/children/footer slots and internal safe area handling. Store has birdingJourney, goals, termsAccepted fields. Stack uses fade animation.</done>
</task>

<task type="auto">
  <name>Task 2: Delete old screen files no longer in flow</name>
  <files>
    app/(onboarding)/location.tsx,
    app/(onboarding)/interests.tsx,
    app/(onboarding)/notifications.tsx,
    app/(onboarding)/summary.tsx,
    app/(onboarding)/skill-level.tsx
  </files>
  <action>
Delete the following files that are replaced by new screens in the updated flow:
- `app/(onboarding)/location.tsx` -- location screen removed from flow
- `app/(onboarding)/interests.tsx` -- replaced by goals.tsx
- `app/(onboarding)/notifications.tsx` -- removed from flow
- `app/(onboarding)/summary.tsx` -- removed from flow
- `app/(onboarding)/skill-level.tsx` -- replaced by birding-journey.tsx

Use `rm` to delete each file. These files may have imports to the old store fields (setLocation, setSkillLevel, setInterests) which is why they must be deleted before TypeScript will pass with the updated store.

After deletion, verify no other files import from these deleted screens (they shouldn't -- navigation is via router.push string paths, not imports).
  </action>
  <verify>
    <automated>cd /Users/joaobborges/Developer/BIRDADEMO && ls app/(onboarding)/location.tsx app/(onboarding)/interests.tsx app/(onboarding)/notifications.tsx app/(onboarding)/summary.tsx app/(onboarding)/skill-level.tsx 2>&1 | grep -c "No such file"</automated>
  </verify>
  <done>All 5 old screen files are deleted. TypeScript compiles without errors referencing deleted files or removed store fields.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `OnboardingLayout.tsx` exports `OnboardingLayout` function
- Store has `birdingJourney`, `goals`, `termsAccepted` fields
- `_layout.tsx` has `animation: 'fade'`
- Old screen files (location, interests, notifications, summary, skill-level) do not exist
- No file in the project imports `useSafeAreaInsets` from onboarding screen files (only from OnboardingLayout)
</verification>

<success_criteria>
- OnboardingLayout component created with header/children/footer slot pattern
- Zustand store updated with new fields (birdingJourney, goals, termsAccepted) and old fields removed
- Stack navigator uses fade animation
- 5 old screen files deleted
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-onboarding-and-paywall/02-01-SUMMARY.md`
</output>
