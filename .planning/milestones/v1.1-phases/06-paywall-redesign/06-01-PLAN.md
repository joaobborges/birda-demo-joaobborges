---
phase: 06-paywall-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(onboarding)/paywall.tsx
autonomous: false
requirements:
  - PAYW-04
  - PAYW-05
  - PAYW-06
  - PAYW-07
  - PAYW-08
  - PAYW-09
  - PAYW-10

must_haves:
  truths:
    - "Paywall displays a full-bleed hero placeholder at ~35% screen height with a close (x) button in top-right"
    - "Paywall shows 'Unlock the full experience' headline and 3 feature bullets with ribbon icons"
    - "Paywall shows two social proof placeholder boxes side by side"
    - "Paywall shows pricing (3,33/month large, annual line small) with unlock pill above"
    - "CTA reads 'Redeem your FREE Week' with FREE in bold"
    - "Footer shows Terms of Use, Restore Purchase, Privacy Policy as tappable links"
    - "Close button and CTA both call completeOnboarding() then router.replace('/(main)') to land on home map"
    - "Back gesture does not return to paywall after dismiss"
  artifacts:
    - path: "app/(onboarding)/paywall.tsx"
      provides: "Complete paywall screen rewrite"
      contains: "router.replace"
  key_links:
    - from: "app/(onboarding)/paywall.tsx"
      to: "src/stores/onboarding.ts"
      via: "useOnboardingStore completeOnboarding"
      pattern: "completeOnboarding\\(\\)"
    - from: "app/(onboarding)/paywall.tsx"
      to: "/(main)"
      via: "router.replace"
      pattern: "router\\.replace.*main"
---

<objective>
Rewrite the paywall screen as a standalone, conversion-oriented full-screen layout with hero image placeholder, feature bullets, social proof, pricing, CTA, and footer links. Dismissing navigates to the home map screen.

Purpose: Replace the current OnboardingLayout-based paywall with a polished, fixed-height standalone screen matching the new design spec.
Output: Fully rewritten `app/(onboarding)/paywall.tsx`
</objective>

<execution_context>
@/Users/joaobborges/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaobborges/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-paywall-redesign/06-CONTEXT.md
@.planning/phases/06-paywall-redesign/06-RESEARCH.md

@app/(onboarding)/paywall.tsx
@src/stores/onboarding.ts
@src/theme/colors.ts
@src/theme/typography.ts
@src/theme/spacing.ts
@src/theme/components.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/stores/onboarding.ts:
```typescript
export const useOnboardingStore = create<OnboardingState>()(...)
// Available selector: (s) => s.completeOnboarding
// completeOnboarding: () => set({ completed: true })
```

From src/theme/components.ts:
```typescript
export const buttons = {
  cta: {
    paddingVertical: 16,
    paddingHorizontal: 24,
    backgroundColor: colors.blue500,
    borderRadius: borderRadius.full,
    borderCurve: 'continuous' as const,
  },
}
```

From src/theme/colors.ts:
```typescript
export const semantic = {
  actionPrimary: colors.blue500,      // #1F87FE — icon color, CTA bg
  actionPrimaryBg: colors.blue50,     // #C7E1FF — unlock pill bg
  actionSecondaryText: colors.neutral0, // white — CTA text
  bgPage: colors.neutral0,            // white — page bg
  bgTinted: colors.blue50,            // blue50 — hero placeholder bg
  textPrimary: colors.neutral700,     // #2D3142 — headline
  textBody: colors.neutral650,        // body text
  textMuted: colors.neutral400,       // footer links
  markerCommon: colors.markerCommon,   // #22C55E — green checkmark
}
```

From src/theme/typography.ts (expected exports):
```typescript
export const typography = { h2, bodySmall, caption, ... }
export const fontWeights = { regular, semiBold, bold, ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite paywall screen with standalone layout</name>
  <files>app/(onboarding)/paywall.tsx</files>
  <action>
Complete rewrite of `app/(onboarding)/paywall.tsx`. Remove ALL existing code (OnboardingLayout, pricing toggle, variants, Reanimated animations). Build a standalone full-screen layout with three zones.

**Imports needed:**
- `View`, `Text`, `Pressable`, `StyleSheet`, `Dimensions` from `react-native`
- `useSafeAreaInsets` from `react-native-safe-area-context`
- `useRouter` from `expo-router`
- `Ionicons` from `@expo/vector-icons`
- `useOnboardingStore` from `@/stores/onboarding`
- `semantic`, `colors` from `@/theme/colors`
- `typography`, `fontWeights` from `@/theme/typography`
- `spacing` from `@/theme/spacing`
- `buttons` from `@/theme/components`

**Constants:**
- `HERO_HEIGHT = Dimensions.get('window').height * 0.35`
- `FEATURES` array: `['Offline maps and field guides', 'Rare bird alerts in your area', 'AI sighting reports']`

**handleDismiss function** (used by BOTH close button AND CTA):
```
completeOnboarding()       // store update FIRST
router.replace('/(main)')  // then navigate
```

**Zone 1 — Hero (fixed height):**
- Root `View` with `height: HERO_HEIGHT`, `position: 'relative'`, `overflow: 'hidden'`
- Placeholder child: `View` with `StyleSheet.absoluteFillObject` and `backgroundColor: semantic.bgTinted`
- Close button: `Pressable` absolutely positioned at `right: 16`, `top: insets.top + 12`, `zIndex: 10`
  - Renders children function pattern: `({ pressed }) => (...)` with `opacity: pressed ? 0.7 : 1`
  - Inner circle: 36x36, `borderRadius: 18`, `borderCurve: 'continuous'`, `backgroundColor: 'rgba(0,0,0,0.35)'`, centered content
  - `Ionicons name="close" size={20} color="#FFFFFF"`
  - `onPress={handleDismiss}`

**Zone 2 — Body (flex: 1):**
- `paddingHorizontal: 24`, `paddingTop: 20`
- Use `justifyContent: 'space-evenly'` so React Native distributes space across device sizes (critical for iPhone SE fit)
- Sections in order:

1. **Headline**: `Text` with `typography.h2`, `color: semantic.textPrimary`, `textAlign: 'center'`, `letterSpacing: -0.5`. Content: "Unlock the full experience"

2. **Feature bullets**: Map over FEATURES array. Each row is `flexDirection: 'row'`, `alignItems: 'center'`, `gap: spacing['3']`. Icon: `Ionicons name="ribbon" size={18} color={semantic.actionPrimary}`. Text: `typography.bodySmall`, `color: semantic.textBody`. Wrap all rows in a container `View` with `gap: 14` and `alignItems: 'flex-start'`, centered with `alignSelf: 'center'`.

3. **Social proof row**: `flexDirection: 'row'`, `justifyContent: 'center'`, `alignItems: 'center'`, `gap: 16`. Two placeholder `View` boxes, each `width: 140`, `height: 72`, `backgroundColor: semantic.bgTinted`, `borderRadius: 12`, `borderCurve: 'continuous'`. Add code comments: `{/* Replace with Image when asset provided — maintain 140x72 dimensions */}`

4. **Unlock pill**: `flexDirection: 'row'`, `alignItems: 'center'`, `gap: 6`, `alignSelf: 'center'`, `backgroundColor: semantic.actionPrimaryBg`, `paddingVertical: 6`, `paddingHorizontal: 14`, `borderRadius: 999`, `borderCurve: 'continuous'`. Icon: `Ionicons name="lock-open-outline" size={14} color={semantic.actionPrimary}`. Text: "Unlock all features", `fontFamily: fontWeights.semiBold`, `fontSize: 13`, `color: semantic.actionPrimary`.

5. **Pricing block** (wrapper View with `alignItems: 'center'`, `gap: 4`):
   - Large price: `fontFamily: fontWeights.bold`, `fontSize: 32` (literal — non-token per project convention, add comment), `color: semantic.textPrimary`. Content: "3,33 /month" with the euro sign.
   - Annual line: `typography.caption`, `color: semantic.textMuted`. Content: "39,99 billed annually after trial" with euro sign.

6. **Trust line**: `flexDirection: 'row'`, `alignItems: 'center'`, `gap: 6`, `alignSelf: 'center'`. Icon: `Ionicons name="checkmark-circle" size={16} color={colors.markerCommon}`. Text: "No payment required . Cancel anytime", `fontFamily: fontWeights.regular`, `fontSize: 12`, `color: semantic.textMuted`. Use middle dot character (U+00B7) as separator.

**Zone 3 — Footer:**
- `paddingHorizontal: 16`, `paddingBottom: insets.bottom + 24`, `gap: 12`

7. **CTA button**: Custom `Pressable` (NOT Button.tsx — mixed weight text needed). Style: spread `buttons.cta`, add `alignItems: 'center'`. Use `({ pressed }) => [style, { opacity: pressed ? 0.85 : 1 }]` pattern. Inner text: `fontFamily: fontWeights.semiBold`, `fontSize: 15`, `color: semantic.actionSecondaryText`. Content uses nested Text: `Redeem your ` then `<Text style={{ fontFamily: fontWeights.bold }}>FREE</Text>` then ` Week`. `onPress={handleDismiss}`.

8. **Footer links**: `flexDirection: 'row'`, `justifyContent: 'center'`, `alignItems: 'center'`. Three `Pressable` wrapping `Text` ("Terms of Use", "Restore Purchase", "Privacy Policy") with `fontFamily: fontWeights.regular`, `fontSize: 12`, `color: semantic.textMuted`. Separated by non-interactive `Text` elements containing " . " (middle dot). Each Pressable `onPress` is a no-op `() => {}` (visual demo).

**StyleSheet:** Use `StyleSheet.create()` at bottom of file. Apply `borderCurve: 'continuous'` on ALL rounded corners per STYL-01 convention. Use ternary with `null` for any conditional rendering (never `&&`).

**Important constraints:**
- Do NOT use OnboardingLayout — standalone layout per user decision
- Do NOT add ScrollView — content must fit on one screen
- Do NOT use Button.tsx for CTA — need mixed-weight text
- Do NOT use `router.push` — must use `router.replace` to prevent back navigation
- Use `spacing['3']` (12px) between body sections if space-evenly doesn't work well — avoid spacing['6'] (24px) which may overflow on small screens
  </action>
  <verify>
    <automated>cd /Users/joaobborges/Developer/BIRDADEMO && npx expo export --platform ios 2>&1 | tail -5</automated>
  </verify>
  <done>
- paywall.tsx is a standalone full-screen layout (no OnboardingLayout import)
- Hero placeholder renders at 35% height with close (x) button using safe area insets
- 3 feature bullets with Ionicons ribbon icons
- Social proof placeholders (140x72 each) side by side
- Unlock pill with lock-open-outline icon
- Pricing block with large monthly and small annual text
- Trust line with green checkmark-circle icon
- CTA reads "Redeem your FREE Week" with FREE bolded via nested Text
- Footer links row with Terms/Restore/Privacy separated by dots
- Both close button and CTA call completeOnboarding() then router.replace('/(main)')
- No ScrollView, no pricing toggle, no variant logic
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of paywall</name>
  <action>User verifies the complete paywall redesign visually and functionally in Expo Go.</action>
  <what-built>Complete paywall redesign with hero placeholder, feature bullets, social proof, pricing, CTA, and footer links. Close button and CTA both navigate to home map screen.</what-built>
  <how-to-verify>
    1. Open Expo Go and navigate to the paywall screen (go through onboarding flow to reach it)
    2. Verify hero placeholder (blue-tinted rectangle) takes up roughly top third of screen
    3. Verify close (x) button is visible in top-right, not hidden under notch
    4. Verify "Unlock the full experience" headline is centered below hero
    5. Verify 3 feature bullets with blue ribbon icons are visible
    6. Verify two placeholder boxes for social proof are side by side
    7. Verify "Unlock all features" pill with lock icon is centered
    8. Verify pricing shows large monthly rate and smaller annual line
    9. Verify green checkmark trust line ("No payment required...") is visible
    10. Verify CTA button reads "Redeem your FREE Week" with FREE visually bolder
    11. Verify footer links (Terms, Restore, Privacy) are visible at bottom
    12. Tap the close (x) button -- should navigate to home map screen
    13. Attempt back gesture -- should NOT return to paywall
    14. Reset onboarding state and navigate to paywall again
    15. Tap "Redeem your FREE Week" CTA -- should also navigate to home map screen
    16. Check that all content fits on screen without any clipping (especially on smaller devices if available)
  </how-to-verify>
  <verify>Visual and functional inspection in Expo Go confirms all 16 checks pass</verify>
  <done>User confirms paywall looks correct and both dismiss actions navigate to home map screen</done>
  <resume-signal>Type "approved" or describe any visual/functional issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npx expo export --platform ios` completes without errors
- paywall.tsx has no imports from OnboardingLayout
- paywall.tsx contains `router.replace('/(main)')` (not `router.push`)
- paywall.tsx contains `completeOnboarding()` called before `router.replace`
- All content sections (1-10 from context) are present in the file
</verification>

<success_criteria>
- Paywall screen renders as a standalone full-screen layout with all 10 content sections
- Close button and CTA both dismiss to home map screen with no back-stack return
- All visual elements match the spec: hero, headline, bullets, social proof, pricing, trust line, CTA, footer links
- No scroll behavior -- all content fits on one screen
</success_criteria>

<output>
After completion, create `.planning/phases/06-paywall-redesign/06-01-SUMMARY.md`
</output>
