---
phase: 15-image-wiring
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/welcome/BirdMosaic.tsx
  - src/components/map/BirdInfoCard.tsx
  - src/components/map/BirdDrawerContent.tsx
  - app/bird-detail.tsx
  - app/(main)/community.tsx
  - src/components/map/BirdMarker.tsx
  - app/(onboarding)/name.tsx
autonomous: true
gap_closure: true
requirements: [IMG-01, IMG-04, IMG-05]

must_haves:
  truths:
    - "Bird images show the bird's face/head visible in all container shapes"
    - "Tapping a bird pin on the map opens the BirdInfoCard bottom sheet"
    - "Name screen heading matches the typography style of other onboarding screens"
  artifacts:
    - path: "src/components/map/BirdMarker.tsx"
      provides: "Tappable map markers without oversized hit area wrapper"
    - path: "src/components/welcome/BirdMosaic.tsx"
      provides: "Bird images with contentPosition='top' for head visibility"
    - path: "app/(onboarding)/name.tsx"
      provides: "Heading using typography.h3 matching other onboarding screens"
  key_links:
    - from: "src/components/map/BirdMarker.tsx"
      to: "Marker onPress"
      via: "react-native-maps native annotation"
      pattern: "onPress.*bird"
---

<objective>
Close 3 UAT gaps from phase 15: fix bird image cropping (contentPosition), restore map pin tappability (remove hitArea wrapper), and fix name screen heading style.

Purpose: All 3 issues were identified during user acceptance testing. The map pin regression is major (blocks bird detail access); image cropping is minor but affects visual quality; heading style is cosmetic consistency.
Output: All 3 gaps resolved, app matches expected behavior.
</objective>

<execution_context>
@/Users/joaobborges/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaobborges/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-image-wiring/15-UAT.md
@.planning/debug/bird-image-cropping.md
@.planning/debug/map-pins-not-tappable.md
@.planning/debug/name-screen-font-weight.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix map pin tappability by removing hitArea wrapper</name>
  <files>src/components/map/BirdMarker.tsx</files>
  <action>
Remove the 44x44 hitArea wrapper View from BirdMarker.tsx. The Marker's children should be the marker shape Views directly (commonMarker, uncommonMarker, rareMarker) without any wrapping container.

Specifically:
1. Remove the `<View style={styles.hitArea}>` wrapper and its closing `</View>` (lines 21, 31)
2. Delete the `hitArea` style from the StyleSheet (lines 37-42)
3. Keep the Marker props as-is (coordinate, tracksViewChanges, anchor, onPress)

The marker shapes (14-18px) become the direct children of Marker, restoring the original native annotation size. This fixes the overlapping transparent annotation issue that broke tap detection on iOS.

Do NOT add any alternative hit-slop mechanism — the original 14-18px markers worked correctly before the regression.
  </action>
  <verify>
    <automated>npx expo export --platform ios 2>&1 | tail -5</automated>
  </verify>
  <done>BirdMarker renders marker shapes directly as Marker children without hitArea wrapper. Builds without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add contentPosition top to all bird image components</name>
  <files>src/components/welcome/BirdMosaic.tsx, src/components/map/BirdInfoCard.tsx, src/components/map/BirdDrawerContent.tsx, app/bird-detail.tsx, app/(main)/community.tsx</files>
  <action>
Add `contentPosition="top"` to every expo-image Image component that displays bird photos in these 5 files. This ensures bird heads (typically in the top portion of portrait-oriented photos) remain visible when images are center-cropped into landscape containers.

Specific changes:
1. BirdMosaic.tsx — add contentPosition="top" to BOTH Image components (the hero image around line 94 and the grid tile image around line 115)
2. BirdInfoCard.tsx — add contentPosition="top" to the Image around line 29
3. BirdDrawerContent.tsx — add contentPosition="top" to the Image around line 31
4. bird-detail.tsx — add contentPosition="top" to the Image around line 55
5. community.tsx — add contentPosition="top" to the Image around line 33

Do NOT change contentFit="cover" — that must remain. Only ADD contentPosition="top" alongside it.
Do NOT modify onboarding illustration images or the name screen avatar — those are not bird photos.
  </action>
  <verify>
    <automated>npx expo export --platform ios 2>&1 | tail -5</automated>
  </verify>
  <done>All 5 bird-image-consuming components have contentPosition="top" alongside contentFit="cover". Bird heads remain visible in all container shapes.</done>
</task>

<task type="auto">
  <name>Task 3: Fix name screen heading to use typography.h3</name>
  <files>app/(onboarding)/name.tsx</files>
  <action>
Replace the heading style in name.tsx to use typography.h3 instead of typography.subheading with bold/fontSize overrides.

In the styles StyleSheet, change the `heading` style from:
```
heading: {
  ...typography.subheading,
  fontFamily: fontWeights.bold,
  fontSize: 24,
  color: semantic.textPrimary,
  textAlign: 'center',
},
```

To:
```
heading: {
  ...typography.h3,
  color: semantic.textPrimary,
  textAlign: 'center',
},
```

This gives the heading Rubik_400Regular at 26px with lineHeight 31, matching the goals.tsx onboarding screen pattern. Remove the `fontWeights` import if it is no longer used elsewhere in the file.
  </action>
  <verify>
    <automated>npx expo export --platform ios 2>&1 | tail -5</automated>
  </verify>
  <done>Name screen heading uses typography.h3 (Rubik_400Regular/26px) matching other onboarding screen headings. No bold override, no manual fontSize.</done>
</task>

</tasks>

<verification>
All three gap fixes compile without errors. Visual verification requires running the app:
- Map pins: tap any bird pin to confirm bottom sheet opens
- Bird images: check mosaic and cards for bird head visibility
- Name screen: compare heading style with goals/discover screens
</verification>

<success_criteria>
1. Map bird pins are tappable again (BirdInfoCard opens on tap)
2. Bird photos show heads/faces in all image containers
3. Name screen heading matches other onboarding screen heading style
</success_criteria>

<output>
After completion, create `.planning/phases/15-image-wiring/15-03-SUMMARY.md`
</output>
